# 요청: strpro(ATRsite-pro) 로컬 개발 — 계산 엔진 교정 + 서버 골격 + KIS 연동 준비

## 작업 시작 전에
같은 폴더에 있는 `strpro_design_spec.md`를 처음부터 끝까지 읽어줘. 이건 기존
ATRsite(`C:\atrsite`, GitHub `bestakas-crypto/atrsite`)를 서버형으로 확장하는
GPT+제미나이 합작 설계 보고서다. 이번 프롬프트의 모든 지시는 이 스펙 문서를
기준으로 한다. 스펙과 이 프롬프트 지시가 충돌하면 이 프롬프트를 우선한다
(로컬 개발 범위 제한 등).

## 프로젝트 위치
- 로컬 디렉토리: **`C:\strpro`** (기존 `C:\atrsite`와는 별개의 새 프로젝트 — 헷갈리지
  않도록 정확히 이 경로에 생성할 것)
- 아직 GitHub 저장소는 없음. 로컬 작업이 어느 정도 진행된 뒤 저장소 생성 여부는
  별도로 물어볼 것이니, 이번 프롬프트에서는 git init/커밋까지만 하고 원격 push는
  하지 말 것

## 이번 작업의 범위 (중요 — 로컬 개발만, 오라클 클라우드는 아직 없음)
오라클 클라우드 서버는 아직 생성되지 않았다. **이번 작업은 전부 로컬 PC(Windows)에서
개발·테스트까지만 진행한다.** 아래는 하지 말 것.
- 실제 systemctl 명령 실행이나 원격 서버 접속 시도
- 오라클 클라우드 관련 실제 배포 작업
다만 나중에 배포할 때 쓸 `systemd/*.service` 파일, `.env.example`, `DEPLOY.md`는
**파일로 준비**해두는 것까지는 이번 범위에 포함(실행은 안 함).

## 참조할 기존 자료 (반드시 확인할 것)
- **TR 코드/요청 명세**: `C:\strpro\manual` 폴더에 있음. KIS API의 TR ID, 요청/응답
  파라미터, 헤더 구성 등을 새로 조사하지 말고 이 폴더의 문서를 최우선으로 참조할 것
- **브로커 연동 예제 코드/라이브러리**: `C:\mmean`을 참조할 것. 이미 실전에서 검증된
  KIS 인증(토큰 발급/갱신), 시세 조회, WebSocket 연결 등의 코드가 있으므로, 3단계
  `adapters/kis_client.py`를 작성할 때 이 코드의 인증 처리 방식과 요청 패턴을
  최대한 그대로 재사용하거나 참고할 것. 단, MMEAN은 선물 전용으로 짜여 있으므로
  현물 시세 조회 TR로 바꿔야 하는 부분은 `C:\strpro\manual`의 TR 명세를 기준으로
  판단할 것
- `C:\mmean`의 코드를 참고하되 그대로 복사해서 strpro 저장소 안에 넣지는 말 것 —
  필요한 인증/통신 로직만 strpro 스타일(비동기 여부, 함수 시그니처 등)에 맞게
  다시 작성할 것. 두 프로젝트는 계속 독립적으로 유지되어야 함(이전에 합의한 대로
  MMEAN과 ATRsite 계열은 완전히 별개 시스템)

## 절대 원칙 (스펙 문서 2절과 동일, 재확인)
- 자동 주문·매매 기능 절대 금지. 이 시스템은 "지금이 매매 타이밍이다"를 알려주기만
  한다. 실제 매매는 항상 사람이 HTS/MTS에서 직접 함
- 회원가입/다중 사용자 시스템 없음, 개인 1인용
- 모바일 웹 우선

## 진행 단계 (반드시 이 순서대로, 각 단계마다 테스트 후 다음 단계로)

### 1단계 — 프로젝트 골격 + 계산 엔진 (가장 중요, 제일 꼼꼼하게)
- 스펙 문서 7절의 디렉터리 구조대로 `C:\strpro`에 골격 생성
- 스펙 문서 8절(계산 엔진 최종 명세)의 공식을 **정확히 그대로** 파이썬으로 구현
  - `services/position_engine.py`: 이동평균 원가법 매수/매도 처리 (스펙 4.1, 8.1)
  - `services/atr_engine.py`: Wilder ATR(14), 확정 일봉 기반 (스펙 8.5)
  - `services/signal_engine.py`: 신호 우선순위 + 포지션 상태 선행조건 (스펙 4.3, 9.1~9.4)
    - 미보유 상태에서는 익절/손절 신호가 발생하지 않도록 반드시 방어(스펙 4.3 표 그대로)
    - 다음 매수가는 `last_buy_price` 기준(스펙 4.4, 8.2), 매도가 기준가에 영향 주지 않게
    - 레칫 손절선(스펙 8.4)
  - 초과매도 검증 로직(스펙 4.2) — 매도수량이 보유수량을 넘으면 예외 발생
- 스펙 19절의 "계산 테스트", "신호 테스트", "레칫 테스트"에 나온 케이스를 **전부**
  단위 테스트(pytest)로 작성하고 통과시킬 것. 특히 아래 두 케이스는 스펙 4.1의
  예시와 정확히 일치해야 함
  ```
  10주×100원 매수 → 5주×120원 매도 → 5주×200원 재매수
  → 최종 보유 10주, 평균단가 150원 (133.33원이 아님)
  ```
  ```
  최고가 100, ATR 10, 손절배수 2 → 손절선 80
  최고가 110 → 손절선 90
  이후 ATR이 20으로 커져도(후보 70) 저장된 손절선은 90 유지
  ```

### 2단계 — SQLite + FastAPI + 프론트엔드 연동 (시세는 아직 수동 입력)
- 스펙 10절 스키마대로 SQLite 구성 (WAL, foreign_keys, busy_timeout — 10.3)
- 스펙 7절의 API 라우터 구성대로 종목/거래/예금 CRUD 엔드포인트 작성,
  1단계에서 만든 계산 엔진을 그대로 호출해서 사용
- `C:\atrsite`의 기존 프론트엔드(index.html/css/js)를 `C:\strpro\frontend`로 복사해서
  시작점으로 삼고, 스펙 4.9 구조대로 JS 파일을 분리하면서 localStorage 호출을
  전부 REST API(fetch) 호출로 교체
- 스펙 16절대로 "기존 localStorage 데이터 내보내기/가져오기" 기능 구현
  (이번엔 시세는 여전히 수동 입력이므로, 스펙 13.2의 "자동 조회" UI 변경은 3단계로 미룰 것)
- 스펙 4.8대로 서비스워커의 캐시 전략을 `/api/`는 Network Only로 분리
- `uvicorn`으로 로컬 실행해서 기존 앱과 계산 결과가 동일한지 직접 비교 테스트

### 3단계 — KIS API 연동 골격 (C:\strpro\manual과 C:\mmean 참조)
- `adapters/kis_client.py`에 스펙 11절(토큰 관리, 호출 속도 제한, 폴링 정책) 구조를
  구현하되, 인증/토큰 발급 로직은 `C:\mmean`의 기존 구현을 최대한 참고해서 재사용하고,
  현물 시세 조회에 필요한 정확한 TR ID와 요청/응답 필드는 `C:\strpro\manual`의 문서를
  따를 것. 그래도 특정 TR의 세부 파라미터가 불명확하면 추측해서 채우지 말고,
  함수 시그니처와 TODO 주석만 만들어두고 다음 형태로 비워둘 것. 예:
  ```python
  def get_current_price(instrument) -> Quote:
      """
      TODO: manual 폴더의 [해당 TR ID] 명세와 C:\\mmean의 인증/요청 패턴을 참고해서
      실제 KIS 시세조회 TR 호출 코드를 채울 것.
      임시로는 테스트용 더미 데이터를 반환하도록 구현해서 나머지
      파이프라인(ATR 계산, 신호 판정)이 이 함수 결과만으로 정상 동작하는지
      먼저 검증할 것.
      """
  ```
- `worker.py`에 스펙 11.3 폴링 정책(정규장 5분 간격, 장 시작 전/마감 후 처리)과
  스펙 11.4 거래일 판정 구조를 골격만 구현 (실제 공휴일 데이터는 최소한의
  하드코딩 테이블로 시작해도 됨)
- 이 단계에서는 위 더미 데이터로 파이프라인 전체(시세→ATR→신호)가 끊김 없이
  돌아가는지 로컬에서 확인하는 데 집중. 텔레그램 알림은 아직 연결하지 말 것

### 4단계 — 알림 Outbox + 텔레그램 골격
- 스펙 12절(Outbox 구조, 재시도 정책, 메시지 형식)대로 구현
- Bot Token/Chat ID는 `.env`에서 읽도록, 값이 없으면 알림을 콘솔 로그로만 출력하는
  더미 모드로 동작하게 만들어서 실제 토큰 없이도 로직 테스트가 가능하게 할 것

### 5단계 — 배포 준비 파일(실행은 안 함)
- 스펙 6절 기술스택 기준 `requirements.txt`
- 스펙 17.2 systemd 유닛 파일들 (`systemd/*.service`, `*.timer`)
- `.env.example` (KIS_APP_KEY, KIS_APP_SECRET, KIS_ACCOUNT_NO, KIS_IS_PAPER_TRADING,
  TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, API_KEY 등 항목명만, 값은 비움)
- `.gitignore`에 `.env`, `*.db`, `backups/` 추가
- `DEPLOY.md`: 나중에 오라클 클라우드 서버가 준비되면 따라 할 배포 절차 문서화
  (SSH 접속 → clone → 가상환경 → 값 채우기 → systemd 등록 순서, 스펙 15절 백업
  정책도 포함)

## 완료 후
- 각 단계가 끝날 때마다 실행한 테스트 결과(통과한 케이스 목록)를 요약해서 알려줄 것
- `C:\strpro`에 `history.txt`를 새로 만들어 `C:\atrsite\history.txt`와 같은 형식으로
  이번 작업 내용을 기록 시작할 것
- 로컬 git 커밋까지만 하고 원격 저장소 연결은 하지 말 것
- 작업량이 매우 크니 1단계부터 순서대로 진행하고, 각 단계 테스트를 통과한 뒤에만
  다음 단계로 넘어갈 것. 중간에 막히면 어느 단계에서 무엇이 막혔는지 명확히 보고할 것
