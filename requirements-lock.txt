# ATRsite-pro 운영(배포) 전체 의존성 잠금 파일 -- 2026-08-06 추가.
#
# requirements.txt는 직접 의존성 5개만 고정하고, starlette/httpcore/anyio 같은
# 간접 의존성은 설치 시점에 따라 달라질 수 있었다(GPT 코드리뷰 지적). 이
# 파일은 requirements.txt를 깨끗한 Python 3.12.3 venv(RackNerd VPS,
# /usr/bin/python3.12, 2026-08-06 생성)에 새로 설치해서 나온 전체 트리를
# `pip freeze`로 그대로 고정한 것이다.
#
# 재생성 방법:
#   python3.12 -m venv /tmp/lockcheck-prod
#   /tmp/lockcheck-prod/bin/pip install -r requirements.txt
#   /tmp/lockcheck-prod/bin/pip freeze > requirements-lock.txt
#
# 배포 시 사용법(선택): pip install -r requirements-lock.txt
# (requirements.txt만 써도 동작은 하지만, 서버 재설치 때 정확히 같은 버전
# 조합을 재현하려면 이 파일을 쓴다.)

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
