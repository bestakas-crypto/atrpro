====================================================================
  ATRsite-pro (strpro) 개발 히스토리
====================================================================
작성일: 2026-08-02
관련 문서:
  - strpro_design_spec.md   (GPT+Gemini 합작 설계 보고서, 전체 근거 문서)
  - claude_code_prompt_strpro.md  (이번 작업 실제 지시 프롬프트, 스펙과
    충돌 시 이 프롬프트를 우선)
관련 저장소:
  - 기존 ATRsite: https://github.com/bestakas-crypto/atrsite (C:\atrsite,
    순수 클라이언트 PWA, localStorage만 사용, 별개 시스템으로 계속 유지)
  - 이 프로젝트: https://github.com/bestakas-crypto/atrpro (원격 연결/푸시는
    이번 세션 범위 밖 -- 로컬 git 커밋까지만)

이 문서는 ATRsite(C:\atrsite)를 서버형으로 확장한 ATRsite-pro의 개발
기록이다. 오라클 클라우드 서버는 아직 없고, 지금까지 전부 로컬 Windows PC
에서 개발·테스트만 진행했다 (배포 준비 파일은 작성해뒀지만 실행 안 함).

--------------------------------------------------------------------
1. 기술 스택
--------------------------------------------------------------------
- 서버: Python 3.12, FastAPI, uvicorn(단일 워커), 표준 sqlite3 + 얇은
  Repository 계층, httpx (KIS 클라이언트/텔레그램 클라이언트)
- 프런트엔드: 순수 HTML/CSS/JS (ES Modules), 빌드 도구 없음, 기존 라이트
  테마 유지, PWA(manifest.json + service-worker.js)
- DB: SQLite (WAL, foreign_keys=ON, busy_timeout=5000, synchronous=NORMAL)
- 프로세스 분리: 웹(main.py)과 시세 Worker(worker.py)를 별도 프로세스로
  운영, 같은 SQLite DB만 공유 (외부 웹훅/localhost HTTP 호출 없음)
- 로컬 개발 환경: conda env "strpro" (Python 3.12), C:\mmean과 완전히
  독립된 별도 프로젝트

--------------------------------------------------------------------
2. 절대 원칙 (변경 없이 계속 유지)
--------------------------------------------------------------------
- 자동 주문·매매 기능 없음. 이 시스템은 "지금이 매매 타이밍이다"를
  알려주는 의사결정 보조 도구일 뿐, 실제 매매는 항상 사람이 HTS/MTS에서
  직접 한다
- 회원가입/다중 사용자 시스템 없음, 개인 1인용
- 모바일 웹 우선

--------------------------------------------------------------------
3. 개발 히스토리 (단계순)
--------------------------------------------------------------------

[1단계] 프로젝트 골격 + 계산 엔진
--------------------------------------------------------------------
2026-08-01
  - 스펙 7절 디렉터리 구조대로 backend/atrsite/{services,repositories,
    adapters,api}, frontend/, tests/, scripts/, systemd/, migrations/,
    data/, backups/ 골격 생성
  - services/position_engine.py: 이동평균 원가법 매수/매도 (스펙 4.1,
    4.2, 8.1). 매도 후 재매수 시 평균단가가 과거 전체 매수를 기준으로
    잘못 계산되던 기존 앱의 버그를 근본적으로 재설계해서 방지
  - services/atr_engine.py: Wilder ATR(14), 단순이동평균이 아닌 진짜
    Wilder 평활화(스펙 8.5)
  - services/signal_engine.py: 신호 우선순위(동시도달>손절>익절>매수>
    관망), 포지션 상태 선행조건(미보유 시 익절/손절 신호 원천 차단,
    스펙 4.3), 다음 매수가는 last_buy_price 기준(매도가 영향 안 줌,
    스펙 4.4), 레칫 손절선(스펙 8.4)
  - pytest 34개 전부 통과: 스펙 19절 계산/신호/레칫 테스트 전항목,
    특히 "10주x100원매수->5주x120원매도->5주x200원매수->평균단가
    150원"과 "최고가100/ATR10/배수2->손절선80->최고가110->손절선90
    (ATR이 20으로 커져도 안 내려감)" 두 필수 예시를 정확히 재현

[2단계] SQLite + FastAPI + 프런트엔드 연동 (시세는 수동 입력)
--------------------------------------------------------------------
2026-08-01~08-02
  - schema.py: 스펙 10.1 핵심 테이블 전부 + 10.2 제약조건, db.py에
    WAL/foreign_keys/busy_timeout/synchronous 적용(10.3)
  - repositories/*, services/portfolio_service.py: 거래 저장 시마다
    전체 재생(replay)해서 position_state 갱신, 레칫/신호 재계산까지
    한 번에 오케스트레이션 (스펙 4.7 "거래 수정 시 추적 손절 재검증")
  - api/{dashboard,instruments,trades,deposits,legacy,health}.py +
    main.py: FastAPI가 프런트엔드 정적 파일도 함께 서빙(동일 출처,
    스펙 14.1)
  - C:\atrsite 프런트엔드를 frontend/로 이식하고 스펙 4.9 구조대로
    js/ 모듈 분리(api-client/dashboard/instrument-detail/trade-form/
    deposits/formatters/offline-snapshot/legacy-export + 부트스트랩
    main.js), localStorage 호출을 전부 REST fetch로 교체
  - service-worker.js: 스펙 4.8대로 /api/는 Network Only, 정적 자산은
    Network First + 캐시 폴백
  - scripts/import_legacy.py + services/legacy_import.py: 스펙 16절
    기존 localStorage 데이터(stock-portfolio/cash-deposits/fx-rates)
    가져오기, 사전 검증(중복ID/초과매도/날짜형식 등) 후 확인 트랜잭션
  - 실사용 중 발견해서 고친 버그 2건:
    1) SQLite 커넥션이 FastAPI 워커스레드 간 재사용되며
       check_same_thread 오류 발생 -> db.py에 check_same_thread=False
    2) 오프라인 배너 CSS가 [hidden] 속성보다 우선 적용돼 항상 보이던
       문제 -> 기존 코드베이스 관례(.class[hidden]{display:none}) 적용
  - pytest 60개 통과, uvicorn 로컬 구동 + 브라우저로 스펙 필수 예시
    (평균단가 150원)를 실제 UI 입력으로 재현해 확인

[3단계] KIS API 연동 골격
--------------------------------------------------------------------
2026-08-02
  - adapters/kis_client.py: 인증/토큰(TokenCache, 만료 5분 전 캐시
    재사용)과 요청 패턴은 C:\mmean/core/kis_data_api.py의 검증된
    방식을 strpro 스타일로 재작성(복사 아님, 두 프로젝트 계속 독립
    유지). TR ID(FHKST01010100 현재가, FHKST03010100 일봉)는
    C:\strpro\manual로 이미 확인된 값이라 상수로 고정했지만, 실제
    호출 본문은 프롬프트가 지정한 형식 그대로 TODO로 남기고 종목코드
    시드 기반 결정론적 더미 데이터를 반환하도록 구현. 스펙 11.2
    순차 Rate Limiter 포함
  - services/market_schedule.py: 스펙 11.4 거래일 판정(주말 + 최소
    고정공휴일 테이블만 -- 음력 연휴/대체공휴일은 아직 비어있음,
    실배포 전 채워야 함) + 11.3 장 상태(PRE_MARKET/REGULAR/
    POST_MARKET/HOLIDAY) 판정
  - worker.py: `python -m atrsite.worker [--once]`. 정규장 5분 폴링,
    마감 후 확정 일봉 수집+ATR 갱신. 텔레그램은 아직 연결 안 함
  - pytest 78개 통과, CLI로 시세->ATR->신호 파이프라인이 끊김 없이
    도는 것까지 직접 실행해서 확인(더미 매수 10주x70,000원 ->
    익절가/손절선/다음매수가 전부 공식대로 계산되고 신호까지 정상 판정)

[4단계] 알림 Outbox + 텔레그램 골격
--------------------------------------------------------------------
2026-08-02
  - adapters/telegram_client.py: BOT_TOKEN/CHAT_ID가 .env에 없으면
    콘솔 로그만 남기는 더미 모드로 동작(실제 토큰 없이도 재시도
    로직 테스트 가능)
  - services/notification_service.py: 스펙 12.3 메시지 형식(매수 예시
    그대로 + 익절/손절/동시도달로 확장, "추천 수량" 표현 대신 "등록된
    트랜치 금액"/"계산 가능 수량"/"보유수량" 사용), 스펙 12.2 재시도
    백오프(1차 30초/2차 2분/3차 10분/4차 실패 확정)
  - portfolio_service.recompute_signal()이 신호가 알림 대상 상태(매수/
    익절/손절/동시도달)로 전환될 때만 signal_events와
    notification_outbox를 같은 트랜잭션에서 함께 기록 (스펙 12.1).
    관망/미보유로 돌아가는 전환은 알림 안 보냄
  - instruments에 tranche_amount(등록된 트랜치 금액) 필드 추가
  - worker.py 폴링 주기마다 notification_service.process_outbox()를
    별도 커밋 경계로 호출
  - pytest 86개 통과, CLI로 실제 발송(더미 모드) -> SENT까지, 그리고
    4회 재시도 실패 후 확정 실패까지 시간을 강제 진행시켜 전부 확인

[5단계] 배포 준비 파일 (실행은 안 함)
--------------------------------------------------------------------
2026-08-02
  - requirements.txt / requirements-dev.txt: conda env "strpro"에 실제
    설치·검증된 버전으로 고정
  - systemd/atrsite-{web,worker,backup}.service + backup.timer:
    Restart=on-failure/RestartSec=10 + StartLimitIntervalSec/Burst(반복
    재시작 방지, 스펙 17.2), 최소 권한(NoNewPrivileges 등)
  - .env.example, .gitignore(.env/*.db/backups/ 등 추가)
  - DEPLOY.md: SSH접속->clone->가상환경->값채우기->systemd등록 순서 +
    스펙 15절 백업 정책 + 남은 TODO 목록
  - scripts/backup.py, scripts/restore_check.py: 스펙 목록엔 명시적으로
    없었지만 systemd 유닛이 참조하므로 로컬 검증 가능한 범위(Online
    Backup API, 무결성/외래키 검사, gzip, 로컬 최신 2개 로테이션)까지
    실제로 만들고 실행해서 확인. Object Storage 업로드는 오라클 계정이
    아직 없어 TODO로 남김
  - api-client.js에 X-API-Key 헤더 전송 로직이 빠져있던 걸 뒤늦게 발견
    (.env.example 작성 중 API_KEY 설명을 쓰다가 알아챔) -> 배포 후
    API_KEY를 채우는 순간 프런트엔드가 전부 401로 먹통이 될 뻔한 걸
    localStorage 저장 + 401 시 1회 프롬프트 재시도 흐름으로 막음
  - pytest 86개 유지 (배포 파일 위주라 로직 변경 최소)

--------------------------------------------------------------------
4. 파일 구성
--------------------------------------------------------------------
  backend/atrsite/
    main.py               FastAPI 진입점 (API 라우터 + 정적 프런트엔드 서빙)
    worker.py              시세 Worker 진입점 (별도 프로세스)
    config.py               .env 기반 설정
    db.py / schema.py        SQLite 연결·DDL
    repositories/            테이블별 CRUD (계산 없음)
    services/                position_engine / atr_engine / signal_engine /
                              market_schedule / portfolio_service /
                              notification_service / legacy_import
    adapters/                kis_client.py(더미 데이터, TR 호출부 TODO) /
                              telegram_client.py(더미 모드 지원)
    api/                     dashboard / instruments / trades / deposits /
                              legacy / health 라우터
  frontend/                 index.html / css / js(모듈 분리) / manifest.json /
                              service-worker.js
  scripts/                  import_legacy.py / backup.py / restore_check.py
  systemd/                  atrsite-{web,worker,backup}.service, backup.timer
  tests/                    pytest 86개 (backend 전 영역)
  requirements.txt, requirements-dev.txt, .env.example, .gitignore, DEPLOY.md

[마무리 이후] 로컬 커밋 + 원격 푸시
--------------------------------------------------------------------
2026-08-02
  - git init -> 140개 파일 커밋(a24c6d7) -> 원격 https://github.com/
    bestakas-crypto/atrpro 의 main 브랜치에 실제 푸시(사용자가 명시적으로
    요청). C:\strpro\atrpro\ (빈 로컬 git 저장소, 사용자가 미리 만들어둔
    것으로 보임)는 손대지 않고 그대로 둠

[GPT 2차 리뷰 대조 + 실제 결함 4건 수정]
--------------------------------------------------------------------
2026-08-02
  - 사용자가 GPT의 프롬프트/스펙 정합성 리뷰를 전달, "GPT는 실제 코드를
    볼 수 없다"는 전제로 각 지적사항을 실제 코드와 대조 검증
  - GPT 지적 중 실제로는 이미 해결돼 있던 것(허위경보): get_daily_bars()
    존재, repositories/deposits.py·fx.py 존재, 수수료 처리(매수수수료
    원가 포함/매도수수료·세금은 실현손익에서만 차감) 이미 정확히 구현,
    scripts/backup.py·restore_check.py 이미 존재하고 로컬 검증까지 끝남
  - GPT 지적 중 실제로 맞았던 것 4건을 수정:
    1) 스펙 4.3(표: 데이터지연->전체 신호 금지)과 9.1(본문: STALE/
       API_ERROR/INSUFFICIENT_DATA만 금지, DELAYED는 목록에 없음) 간의
       명시적 충돌을 signal_engine.py 모듈 docstring에 명문화 -- 9.1의
       구체적 열거를 따르기로 결정(근거: 17.3의 단계 구분과 일치, 8분
       지연으로 전체 신호를 막는 건 실사용상 과도)
    2) is_buy_signal_active()가 정의만 되고 실제 신호 파이프라인 어디서도
       호출되지 않던 걸 발견(단순 죽은 코드였음, 매수/익절/손절 전부
       히스테리시스 없이 단순 임계값 교차로만 판정되고 있었음) ->
       determine_signal()에 was_hit_buy/was_hit_profit/was_hit_stop 옵션
       인자를 추가해 매수/익절/손절 3종 전부에 히스테리시스(9.4) 실제
       연결. 기본값은 예전과 100% 동일 동작(하위호환), portfolio_service.
       recompute_signal()이 직전 signal_state로부터 이 값들을 채워 넘김
    3) 스펙 17.3 "0~7분 정상/7~15분 지연/15분초과 오래된데이터/연속3회
       실패 API장애" 등급 자동전환이 통째로 미구현이었던 걸 발견(worker.py
       는 항상 FRESH만 기록, 실패해도 상태 갱신 없이 로그만 남김) ->
       quote_latest에 consecutive_failures 컬럼 추가, market_schedule.py에
       compute_data_status() 신설(경과시간+연속실패 기준 동적 계산),
       portfolio_service.recompute_signal()이 KIS 소스 시세에 한해(수동
       입력은 그대로 신뢰) 매번 재계산하도록 연결. worker.py는 조회 실패
       시 즉시 카운터 갱신+재판정(3번째 실패 순간 바로 API_ERROR로 전환,
       다음 성공까지 기다리지 않음). 부수적으로 kis_client.py의 더미
       quoted_at이 naive 로컬시각이라 경과시간 계산이 타임존 불일치로
       틀어질 뻔한 것도 UTC-aware로 통일해서 같이 고침
    4) 리뷰가 지적한 누락 테스트들 추가(fee가 평균단가에 반영/fee·tax가
       실현손익에만 반영, 같은 executed_at에서 sequence_no 순서, 신선도
       자동전환, 연속실패 API_ERROR 전환) 중 "과거 거래 수정으로 초과매도
       발생 시 전체 거부" 테스트를 작성하다가 진짜 버그를 하나 더 발견:
       edit_trade()가 DB에 먼저 쓰고 나중에 재생 검증을 해서, OversellError
       가 나도 이미 쓰여진 부분 수정이 "그 함수 호출만 놓고 보면" DB에
       남아 있었음(API 계층의 트랜잭션 롤백에 의존하고 있었을 뿐, 함수
       자체는 스스로를 보호하지 못함) -> record_trade와 동일하게 메모리
       에서 먼저 replay_trades()로 검증하고 통과해야만 실제로 쓰도록 수정
  - pytest 86 -> 107개로 증가, 전부 통과. 새 결함 발견 2건(위 3, 4)은
    이번에 코드로 직접 고쳤고, 재작성한 테스트(reentry 사이클 관련
    처음 작성한 테스트 하나는 ATR이 사이클에 종속되지 않는다는 걸 잘못
    가정해서 기대값이 틀렸던 것으로 확인 -> 테스트 쪽을 수정)

[신호 확인(acknowledge) UI 추가 -- 사용자 결정 반영]
--------------------------------------------------------------------
2026-08-02
  - 위 히스테리시스 작업에서 "손절 신호가 가격 반등만으로 자동 해제되는
    게 맞는지"는 사용자 결정이 필요하다고 보고했고, 사용자가 "종목별로
    확인(acknowledge) UI를 넣어서, 확인을 누르면 경고/알림이 사라지게
    하자"고 결정
  - signal_state에 latest_event_id(상태 전환마다 갱신)/acknowledged_event_id
    (확인 시점에 latest_event_id로 맞춤) 컬럼 추가. 두 값이 다르면 "미확인"
  - portfolio_service.recompute_signal()을 재배선: signal_events를 signal_state
    갱신보다 먼저 기록해서 그 id를 latest_event_id로 같이 저장. 같은 상태가
    반복 계산될 때는(새 이벤트가 없으므로) 이 값을 건드리지 않아 확인 상태가
    유지됨 -- 상태가 실제로 또 바뀌면(새 이벤트) 자동으로 다시 미확인이 됨
  - POST /api/v1/instruments/{id}/acknowledge 신설. 확인은 신호 상태·손절선
    계산 자체는 전혀 건드리지 않고 배너 표시 여부만 바꿈
  - 프런트엔드: 신호 배너에 "확인" 버튼 추가(매수/익절/손절/동시도달 4종
    트리거 상태에서만 노출, 관망은 확인할 게 없으므로 제외), 확인 후에는
    배너 대신 "확인한 신호입니다..." 안내문 표시
  - 브라우저에서 실제 확인: 손절 신호 발생 -> 배너+확인버튼 노출 -> 확인
    클릭 -> 배너 사라지고 안내문 표시(손절선 표시값은 그대로 80.00 유지)
    -> 같은 손절 상태가 반복 계산돼도(가격이 78로 살짝 더 내려가도) 안 열림
    -> 관망 구간(95)으로 갔다가 다시 손절 구간(79)으로 돌아오면(새 사이클)
    확인 버튼이 다시 뜸 -- 전부 의도대로 동작 확인
  - pytest 107 -> 113개, 전부 통과

[KIS 실전 계좌 연동 -- 시세조회 TR 실제 구현 + 검증]
--------------------------------------------------------------------
2026-08-02
  - 사용자가 .env에 실제 KIS 앱키/시크릿/계좌번호(실전 계좌, 모의투자 아님 --
    처음엔 모의투자로 잘못 설정했다가 사용자가 정정)와 텔레그램 봇 토큰/
    채팅ID까지 채워 넣음
  - openpyxl로 manual 폴더의 두 TR 명세를 정확히 재추출해서(주식현재가
    시세[v1_국내주식-008], 국내주식기간별시세[v1_국내주식-016]) 요청
    파라미터/응답 필드명을 그대로 확인 후 kis_client.py의 TODO를 실제
    구현으로 교체:
    - get_current_price(): FID_COND_MRKT_DIV_CODE=J + FID_INPUT_ISCD ->
      output.stck_prpr(현재가)/stck_hgpr(당일고가)
    - get_daily_bars(): FID_INPUT_DATE_1/2(YYYYMMDD)+FID_PERIOD_DIV_CODE=D+
      FID_ORG_ADJ_PRC=0 -> output2 배열(최신순으로 와서 오름차순 재정렬,
      atr_engine이 과거->최신 순서를 전제하기 때문)
    - telegram_client.py와 동일한 더미 모드 패턴 적용: KIS_APP_KEY/SECRET이
      없으면 여전히 더미 데이터, 있으면 실제 호출 -- pytest가 자격증명 유무와
      무관하게 항상 통과해야 하므로
  - 실전 서버로 토큰 발급 + 삼성전자(005930) 현재가 조회 실제 성공 확인
    (262,500원/당일고가 267,000원, 둘 다 실제 값)
  - 이 환경의 .env에 실제 자격증명이 들어오면서 기존 테스트들이 실제 KIS/
    텔레그램 서버를 호출하려던 문제를 발견하고 즉시 수정: test_kis_client.py/
    test_worker.py/test_notification_service.py에 자격증명을 강제로 비우는
    픽스처 추가. 특히 test_process_outbox_marks_sent_in_dummy_mode가 실제
    텔레그램 메시지를 사용자 채팅방에 보낼 뻔한 것 -- 자동화 테스트를 돌릴
    때마다 가짜 매매신호가 실제로 발송될 뻔한 심각한 문제였음
  - 사고 하나 발생: KIS 응답 원문을 그대로 콘솔에 출력하는 진단 명령을 돌려서
    실제 접근토큰(24시간 유효)이 대화 내용에 그대로 노출됨. 외부 유출은
    아니지만 하지 말았어야 할 실수 -- 이후로는 파싱된 값만 출력하도록 즉시
    수정. 짧은 시간에 새 프로세스를 반복 실행하며 매번 새 토큰을 요청해서
    KIS의 "1일 1회 발급 원칙"과 충돌해 간헐적 403도 발생 -- 실제 운영 중인
    지속 프로세스(main.py/worker.py)는 토큰을 캐시해서 재사용하므로
    문제 없음, 테스트 방식 자체가 원인이었음을 확인
  - pytest 113 -> 124개, 전부 통과. 커밋 전 재확인 완료

[온디맨드 시세 가져오기 + 종목별 KIS 코드 입력 UI]
--------------------------------------------------------------------
2026-08-02
  - 사용자 요청: "5분마다 자동" 폴링(이미 worker.py에 있음)과 별개로 "사용자가
    요청하면 바로 가져오는" 기능도 필요 -- 화면의 기존 "가져오기" 버튼은
    레거시 데이터 마이그레이션용이라 별개임을 설명
  - POST /api/v1/instruments/{id}/quote/refresh 신설: worker의 5분 주기와
    무관하게 즉시 KIS 현재가를 1회 조회해서 반영. ATR/일봉은 여기서 안 건드림
    (장중에 확정 ATR을 임의로 재계산하면 스펙 8.5 원칙이 깨짐)
  - commit_quote()에 day_high 파라미터 추가: 현재가보다 당일 고가가 높으면
    최고가 갱신 기준을 day_high로 씀(스펙 11.3 "현재가와 당일 고가 조회") --
    기존엔 Quote.day_high 필드가 있는데도 아무 데서도 안 쓰이고 있었음
  - 프런트엔드: 종목 설정에 "KIS 종목코드"/"등록된 트랜치 금액" 입력 필드,
    오늘의 입력 영역에 "지금 시세 가져오기" 버튼 추가
  - 브라우저로 실제 전체 흐름 확인: 종목 추가 -> KIS 종목코드(005930) 저장 ->
    "지금 시세 가져오기" 클릭 -> 실제 삼성전자 현재가(262,500)와 당일고가
    (267,000)를 정확히 받아와서 보유 후 최고가가 267,000(현재가 아님)으로
    정확히 갱신됨 -- day_high 반영 로직까지 실사용으로 검증됨
  - pytest 124개 전부 통과

[종목코드 마스터DB 제안 -> 사용자가 기각, 종목 추가 시 KIS 코드 직접 입력으로]
--------------------------------------------------------------------
2026-08-02
  - "종목코드를 사용자가 매번 알아야 하는 게 문제 아니냐"는 사용자 질문에,
    KIS 공식 GitHub(open-trading-api/stocks_info)를 실제로 조회해서 HTS/MTS가
    쓰는 방식(코스피/코스닥 전종목 마스터파일을 주기적으로 다운로드해 로컬
    DB에 저장하고 이름으로 검색)을 확인하고 제안함
  - 사용자가 기각: "혼자 쓰는 프로그램이라 그렇게까지 할 필요 없다. 종목
    등록할 때 KIS 종목코드를 사용자가 직접 입력하게 하라"는 방향으로 정리 --
    마스터DB/검색 기능은 만들지 않음(과설계 방지)
  - "종목 추가" 모달에 종목명/통화만 있고 KIS 코드는 만든 뒤 설정에서 따로
    넣어야 했던 2단계 구조를 사용자가 지적, 종목 추가 시점에 KIS 종목코드도
    같이 입력하도록 모달에 필드 추가
  - 브라우저로 테스트하다가 실제 버그 하나 발견: 새로 추가한 필드가 계속
    빈 값으로 저장됨 -> 원인은 서비스워커의 "네트워크 우선" fetch()가 브라우저
    자체 HTTP 디스크 캐시는 우회하지 못해서(Cache Storage와는 별개 계층),
    파일이 바뀌어도 여전히 예전 trade-form.js를 서빙하고 있었음 ->
    service-worker.js의 모든 fetch() 호출에 `{cache: 'no-store'}`를 추가해서
    브라우저 HTTP 캐시까지 확실히 우회하도록 수정, CACHE_NAME도 v1->v2로
    올림. 예전 atrsite에서 겪었던 "서비스워커가 옛날 파일을 계속 서빙하는"
    문제와 근본적으로 같은 종류의 버그였음
  - pytest 124개 전부 통과 (백엔드 변경 없음, 프런트엔드/서비스워커만 수정)

[가져오기 버튼 위치 이동 + 전체 종목 주가 갱신 버튼]
--------------------------------------------------------------------
2026-08-02
  - 사용자 지적: 목록 화면 상단의 KRW/USD/JPY 통화 전환 버튼이 안 되는 것
    같다고 함 -> 실제로는 정상 동작함을 실데이터로 재확인(매입금액이
    3,490,000원 -> 2,585달러로 환율 반영해서 정확히 바뀜, 활성 버튼 강조도
    정상). 종목/환율이 없는 상태에서 눌러봐서 그렇게 보였을 뿐 버그 아님
  - 사용자 요청: "가져오기"(레거시 마이그레이션, 최초 1회만 쓰는 기능) 버튼을
    눈에 덜 띄는 곳으로 옮기고, 그 자리에 "주가 갱신" 버튼을 넣어서 누르면
    KIS 종목코드가 설정된 모든 종목의 현재가를 한 번에 갱신하게 해달라는 요청
  - services/portfolio_service.py에 refresh_all_quotes_now() 추가: KIS
    종목코드가 있는 종목만 순회하며 refresh_quote_now()를 개별 호출, 하나
    실패해도 나머지는 계속 진행하고 결과(갱신/건너뜀/실패)를 요약해서 반환
    (KisClient의 rate_limiter가 이미 호출 간격을 강제하므로 별도 조절 불필요)
  - POST /api/v1/dashboard/refresh-quotes 신설
  - "가져오기" 버튼은 총계 펼치기(▾) 안의 환율 설정 아래로 이동 -- 자주 안
    쓰는 일회성 기능이라 숨겨도 되는 위치로 판단
  - 브라우저로 사용자의 실제 보유 종목(kodex 200, KIS코드 069500)에 "주가
    갱신" 버튼을 눌러 실제 KIS 서버에서 108,820원을 정상적으로 받아옴 --
    실제 운용 중인 데이터로 전체 흐름 확인 완료
  - pytest 124 -> 126개, 전부 통과

--------------------------------------------------------------------
[QQQ 시세 0원 표시 버그 + 종목추가 모달이 사라지는 버그 수정] (2026-08-02)
--------------------------------------------------------------------
  - 사용자가 QQQ 종목 상세 화면 스크린샷과 함께 두 가지 버그를 신고:
    (1) QQQ 시세는 "가져왔는데" 현재가가 0으로 표시(평가손익 -100%)
    (2) 종목 추가 모달에서 통화를 입력하면 창이 사라짐
  - (1) 원인 규명: get_current_price()는 국내(KRX) 전용 TR
    (FHKST01010100, FID_COND_MRKT_DIV_CODE="J")만 구현되어 있는데, QQQ는
    미국 나스닥 상장 티커라 이 TR로 조회가 안 됨. 실제 KIS 서버에 "QQQ"로
    직접 조회해서 확인한 결과 rt_cd="0"(정상 처리)에 stck_prpr="0"을
    돌려줌 -- 에러가 아니라서 기존 코드가 가격 0을 진짜 시세로 그대로
    커밋해버린 것.
  - (1) 수정: kis_client.py의 get_current_price()/get_daily_bars()에
    price<=0 / close<=0 검증 추가 -- 비정상 응답이면 KisApiError를
    명시적으로 던지도록 함("국내(KRX) 상장 종목 코드가 맞는지 확인하세요.
    해외 종목은 아직 지원하지 않습니다"). 해외주식 TR 자체는 구현하지
    않음 -- 별도 엔드포인트/파라미터가 필요한 꽤 큰 범위 추가라 사용자
    확인 없이 만들지 않음. 이미 저장된 QQQ의 price=0 값은 자동으로
    고쳐지지 않으므로 "지금 시세 가져오기"를 눌러도 계속 실패하고,
    당분간은 QQQ 현재가를 수동 입력해야 함.
  - (2) 원인 규명: 이건 "통화 필드" 자체와는 무관했음. main.js가 모든
    페이지 로드마다 reg.update()로 서비스워커 갱신을 확인하는데,
    service-worker.js는 skipWaiting()+clients.claim()으로 새 버전을
    즉시 활성화하고, main.js의 controllerchange 리스너는 무조건
    location.reload()를 호출했음 -- 사용자가 모달에 입력하는 도중에
    배경에서 새 배포가 반영되면 페이지 전체가 강제로 새로고침되면서
    입력 중이던 모달이 통째로 사라짐. 마침 이번 세션 내내 프론트엔드
    파일을 계속 배포하고 있었어서 타이밍상 "통화 입력할 때" 걸린 것으로
    보임(실제로는 어느 필드든 입력 중이면 재현 가능한 문제).
  - (2) 수정: controllerchange 핸들러가 reload 전에
    document.querySelector('.modal-overlay:not([hidden])')로 열린
    모달이 있는지 확인 -- 있으면 1초 후 다시 확인하는 식으로 미뤘다가,
    모든 모달이 닫힌 뒤에만 reload함.
  - kis_client.py 0원 거부 로직에 대한 테스트 2개 추가
    (test_get_current_price_raises_kis_api_error_on_zero_price,
    test_get_daily_bars_raises_kis_api_error_on_zero_close)
  - pytest 126 -> 128개, 전부 통과
  - 브라우저에서 모달 열어둔 채 .modal-overlay:not([hidden]) 셀렉터가
    실제로 매치되는 것 확인, /js/main.js 응답에 새 로직(reloadWhenIdle)이
    실제로 배포됐는지 fetch로 재확인

--------------------------------------------------------------------
[해외주식(나스닥 등) KIS 시세 TR 추가] (2026-08-02)
--------------------------------------------------------------------
  - 위 QQQ 버그 수정 후, "해외주식 TR을 만들지" 물어봤더니 "필요하다면
    만들어야지. 내 주식은 대부분 미국주식이다" -- 실제로 만듦.
  - manual 폴더에는 해외주식 TR 명세가 없어서, KIS 공식 GitHub
    (koreainvestment/open-trading-api)의 examples_llm/overseas_stock
    예제 코드로 TR ID/파라미터/응답 필드명을 확인:
    - 현재가상세[v1_해외주식-029] TR HHDFS76200200,
      GET /uapi/overseas-price/v1/quotations/price-detail,
      요청 AUTH(빈값)+EXCD(거래소코드)+SYMB, 응답 output.last(현재가)/
      output.high(당일고가)
    - 기간별시세[v1_해외주식-010] TR HHDFS76240000,
      GET /uapi/overseas-price/v1/quotations/dailyprice,
      요청 AUTH+EXCD+SYMB+GUBN(0=일)+BYMD(기준일, YYYYMMDD)+MODP(0),
      응답 output2[].xymd/clos/high/low, 국내 TR과 동일하게 최신순으로
      와서 오름차순 재정렬 필요
  - 실전 계좌로 QQQ/NAS를 직접 조회해서 실제 확인: 현재가 687.99,
    고가 695.77, 저가 680.05 USD, 일봉 100건 정상 수신
  - kis_client.py: get_current_price()/get_daily_bars()가 market
    파라미터(비어있거나 "KRX"면 기존 국내 TR, 그 외 NAS/NYS/AMS 등이면
    새 해외 TR)로 분기하도록 리팩토링. 국내 TR과 동일하게 가격<=0/
    종가<=0이면 KisApiError로 명시적으로 막음(잘못된 종목코드+거래소
    조합 방지)
  - instruments 테이블의 kis_market 컬럼(이전 세션에 스키마만 만들어두고
    안 쓰고 있던 것)을 실제로 연결: worker.py(poll_quotes/
    collect_daily_bars_and_update_atr), portfolio_service.refresh_quote_now
    모두 instrument["kis_market"]을 넘기도록 수정
  - 프런트: 종목 추가 모달 + 종목 설정 패널에 "거래소" select 추가
    (국내KRX/나스닥NAS/뉴욕NYS/아멕스AMS 4개만 -- 필요 이상으로 거래소를
    늘리지 않음). trade-form.js/instrument-detail.js가 kis_market을
    같이 저장하도록 연결
  - 실제로 QQQ 종목에 KIS코드="QQQ"+거래소="NAS"를 저장하고 "지금 시세
    가져오기"를 실행 -> post_entry_high_price가 695.77로 정상 갱신,
    새로고침 후 대시보드에서 QQQ가 -100.00% -> +1.18%로 정상 표시,
    총 손익도 실제 값(+187, +1.23%)으로 정상화된 것을 브라우저에서 직접
    확인
  - kis_client.py 해외 TR 파싱/분기/0원 거부 테스트 4개 추가
  - pytest 128 -> 132개, 전부 통과
  - 아직 안 한 것: worker.py의 5분 자동 폴링은 여전히 국내(KRX) 장중
    시간대(decide_polling)에만 도는 구조라서, 미국 장이 열려 있는
    시간대(한국시간 밤~새벽)에는 자동 갱신이 안 됨 -- 사용자 보유 종목이
    대부분 미국주식이라는 걸 고려하면 이 부분도 필요할 수 있음, 사용자
    확인 필요. 지금은 "지금 시세 가져오기"/"주가 갱신" 수동 버튼은 시간대
    상관없이 항상 동작하니 당장은 그걸로 대응 가능.

--------------------------------------------------------------------
[서버 자동 실행 배치파일(strpro.bat) + ATR 자동 수집 하루 1회 가드] (2026-08-02)
--------------------------------------------------------------------
  - 사용자가 C:\mmean\mmean.bat 같은 자동 실행 배치파일을 요청(한글 쓰면
    코드에러 난다며 영문으로만 만들어달라 함) -- C:\strpro\strpro.bat 작성.
    conda strpro 환경 활성화 -> 웹서버(uvicorn, 8811)와 시세 워커(worker.py,
    무한루프)를 각각 별도 창으로 실행 -> 몇 초 후 브라우저 자동 오픈.
    한글 종목명이 로그에 찍혀도 인코딩 에러 안 나게 chcp 65001 +
    PYTHONUTF8/PYTHONIOENCODING만 넣고, 배치파일 자체 텍스트(주석/echo)는
    전부 영문으로 작성.
  - 이어서 사용자가 "ATR을 자동으로 기록하게 할 방법이 있는가" 질문 --
    사실 worker.py의 collect_daily_bars_and_update_atr()이 3단계부터
    이미 있었음(장마감 후 확정 일봉 조회 -> Wilder ATR(14) 계산 -> 자동
    반영). 문제는 이게 실제로 몇 분 이상 연속으로 돌아간 적이 없어서
    (계속 --once 단발 호출로만 검증) 실사용 조건에서 검증이 안 돼 있었던
    것 -- strpro.bat으로 worker.py가 상시 실행되기 시작하면서 처음으로
    이 gap이 실질적으로 문제가 됨.
  - 코드 리뷰 중 실제 버그 발견: POST_MARKET(장마감 15:30~자정)은
    IDLE_POLL_INTERVAL_SECONDS(60초)마다 run_once()가 도는데, 하루치
    확정 일봉/ATR을 이미 다 반영해놓고도 자정까지 매분 KIS에 똑같은
    일봉 조회를 수백 번 반복하는 구조였음(하루에 한 번만 하면 되는 일을
    ~500번 함) -- KIS 호출 낭비 + 불필요한 API 소모.
  - 수정: worker.py에 모듈 전역 _last_daily_bars_collection_date를 두고,
    run_once()가 같은 날짜에는 collect_daily_bars_and_update_atr()을
    다시 부르지 않도록 가드 추가. 날짜가 바뀌면(다음 거래일 POST_MARKET)
    자동으로 다시 수집됨.
  - 실전 계좌로 국내 TR(get_daily_bars, 삼성전자 005930)도 처음으로
    라이브 검증 -- 이전에는 목업 응답으로만 확인했었음(토큰 발급 한도
    우려로 미뤄뒀던 것). 41개 일봉 정상 수신, 날짜 오름차순 정렬도 정상
    확인. 이전 세션에 이미 검증한 해외 TR(QQQ)까지 합치면 국내/해외 모두
    실전 라이브 검증 완료.
  - tests/test_worker.py: 하루 1회 가드에 대한 autouse 리셋 픽스처 +
    새 테스트 test_run_once_only_collects_bars_once_per_day 추가(같은
    날짜 3번 호출 -> 수집 1번만, 다음 거래일 -> 다시 수집)
  - pytest 132 -> 133개, 전부 통과
  - 결론: ATR은 이미 자동으로 기록되는 구조였고(사용자가 몰랐을 뿐),
    strpro.bat으로 worker.py를 계속 띄워두기만 하면 매 거래일 마감 후
    자동으로 갱신된다. 장중에는 ATR을 절대 재계산하지 않음(스펙 8.5 --
    확정된 일봉으로만 계산해야 다음 매수가/익절가/손절선이 장중에 흔들리지
    않음), 그래서 "지금 시세 가져오기"는 현재가만 갱신하고 ATR은 건드리지
    않는 게 의도된 동작.

--------------------------------------------------------------------
[GPT의 "ATR 자동 기록" 설계 제안 대조 검증 + 실제 갭 2건 수정] (2026-08-02)
--------------------------------------------------------------------
  - 사용자가 GPT가 작성한 ATR 자동화 설계 문서(daily_bars/atr_values 스키마,
    Wilder 증분 갱신, 신규 종목 백필, 서버 다운 시 갭 보충, 수정주가,
    API 장애 처리 등 10개 항목)를 붙여넣고 "판단하고 체크해봐"라고 지시 --
    실제 코드와 하나씩 대조.
  - 이미 구현돼 있던 것(GPT는 코드를 볼 수 없어서 몰랐을 뿐): Wilder
    ATR(14) 계산/최소 15개 일봉 요구/장중 재계산 금지/API 실패 시 기존
    ATR 유지(0으로 안 덮어씀)/MANUAL_OVERRIDE 병행 유지 -- 전부 이미
    정상 동작.
  - 불필요하다고 판단해서 안 만든 것: (a) daily_bars 원본 OHLC 별도
    저장(현재는 worker.py가 매번 60일치를 KIS에서 다시 받아 전체
    재계산하는 구조라, 서버가 며칠 꺼져 있어도 자동으로 갭이 메워짐 --
    GPT가 제안한 "증분 갱신 + 갭 보충 로직"보다 오히려 더 단순하고
    안전함. 원본 봉 감사 로그는 개인용 단일 사용자 앱에 과함) (b) GPT의
    "100~200거래일 확보" 제안 -- Wilder 평활은 오래된 TR의 영향이
    지수적으로 감쇠해서 60일(~40거래일)이면 이미 충분, 더 늘려도 정확도
    이득이 거의 없음(현행 60일 유지) (c) trade_date를 거래소 로컬 날짜로
    저장하라는 제안 -- 이미 KIS 응답 필드(stck_bsop_date/xymd)를 그대로
    쓰고 있어서 KST 변환으로 인한 날짜 꼬임 자체가 애초에 없음.
  - 실제 갭으로 확인해서 고친 것 2건:
    1) 신규 종목 등록 시 ATR 백필 없음 -- 지금까지는 종목을 등록하면
       다음 장마감(최대 하루)까지 ATR이 비어 있어 매수/익절/손절 기준선을
       계산 못 했음. portfolio_service.backfill_atr_now() 신설, 등록
       직후(POST /instruments) + 나중에 설정에서 KIS 코드를 처음 넣을 때
       (PATCH, 이미 ATR이 있으면 덮어쓰지 않음) 자동으로 즉시 백필하도록
       연결. 실패해도(코드 오류 등) 조용히 넘어가고 종목 등록 자체는
       항상 성공. 실전 계좌로 신규 종목(SK하이닉스, 000660)을 만들어서
       워커 없이 즉시 ATR(236,523.82)이 채워지는 것까지 브라우저에서
       확인.
    2) 해외 TR MODP(수정주가반영여부) 파라미터가 반대로 들어가 있었음
       -- GPT의 수정주가 우려를 계기로 KIS 공식 예제와 대조하다가 발견한
       독립적인 실제 버그. 국내 TR(FID_ORG_ADJ_PRC)은 "0=수정주가"인데
       해외 TR(MODP)은 정반대로 "0=미반영/1=반영"이고, 기존 코드는 국내
       관례를 그대로 옮겨써서 "0"(미반영/원주가)을 보내고 있었음 --
       QQQ가 실제로 액면분할하면 과거 봉에 가짜 갭이 생겨 ATR이 왜곡될
       뻔한 버그. "1"로 수정, 실전 계좌로 재검증(수정주가 반영된 값으로
       바뀐 것 확인).
  - 검증 도중 더 심각한 버그를 하나 더 발견(GPT 제안과 무관하게 실사용
    중 실제로 재현됨): kis_client.py의 res.raise_for_status() 5곳이
    전부 httpx 원시 예외(HTTPStatusError 등)를 그대로 던지고 있었음 --
    worker.py/portfolio_service.py는 KisApiError만 잡도록 설계돼 있어서,
    KIS가 HTTP 레벨 오류(예: 토큰 재발급이 몰려서 403)를 주면 그 원시
    예외가 그대로 새어나가 종목 등록 API가 500을 내거나, 최악의 경우
    worker.py의 무한 폴링 루프 전체가 죽어서 수동으로 재시작해야 하는
    상황이었음. 실제로 테스트 중 403이 재현되면서 발견함. TokenCache.get()
    + 새 _get_json() 헬퍼로 httpx.HTTPError를 전부 KisApiError로 감싸도록
    고쳐서 근본 원인을 막음(개별 호출부를 하나하나 고칠 필요 없이 이
    한 지점만 고치면 전체가 안전해짐).
  - tests/test_kis_client.py: MODP="1" 검증, HTTP 원시 예외 -> KisApiError
    변환 검증(TokenCache/get_current_price 각 1개) 추가
  - tests/test_api.py: 종목 등록 시 즉시 ATR 백필/코드 없으면 비어있음/
    설정에서 나중에 채워도 백필/이미 ATR 있으면 안 덮어씀 -- 4개 추가
  - pytest 133 -> 140개, 전부 통과

--------------------------------------------------------------------
5. 아직 안 된 것 (다음에 이어서 할 일)
--------------------------------------------------------------------
  - market_schedule.py의 음력 연휴/대체공휴일/수능일 개장시간 변경 테이블
  - scripts/backup.py, restore_check.py의 Oracle Object Storage 연동
    (계정 생성 후)
  - 오라클 클라우드 인스턴스 생성 + DEPLOY.md대로 실제 배포 (이번
    세션에서는 하지 않음)
  - CSRF 방어, 인증 실패 횟수 제한(스펙 14.1) -- 아직 미구현
  - Shadow Mode(더미 파이프라인 vs 수동입력 공식 비교 테스트) -- 지금까지는
    CLI로 손계산 대조만 했고 formal 테스트로는 안 만듦
  - (해결됨) get_daily_bars() 실서버 검증 -- 국내(삼성전자 41개 일봉)/해외
    (QQQ 100개 일봉) 모두 완료
  - worker.py를 실제로 몇 분 이상 계속 띄워서 5분 주기 자동 폴링이 진짜
    반복되는 걸 지켜본 적은 아직 없음 (run_once() 단발 호출로만 각 단계
    검증했음) -- 실제로 오래 띄워보는 건 다음에
  - (해결됨 -- 아래 "해외주식 TR 추가" 절 참고) QQQ 등 해외 종목은 이제
    거래소를 NAS/NYS/AMS로 설정하면 정상 조회됨
  - worker.py의 5분 자동 폴링이 국내(KRX) 장중 시간대에만 도는 문제 --
    사용자 보유 종목 대부분이 미국주식이라 미국 장중(한국시간 밤~새벽)
    자동 갱신이 안 됨. 수동 갱신 버튼으로는 당장 대응 가능하나, 자동
    폴링 시간대 확장이 필요한지는 사용자 확인 필요
  - VPS에는 배포했지만(아래 절 참고) .env의 KIS/텔레그램 실제 값은 아직
    안 채움 -- 사용자가 서버에 직접 SSH로 들어가서 입력해야 함(더미
    모드로만 떠 있는 상태)
  - Oracle Object Storage 원격 백업 업로드는 여전히 미구현 -- 지금은
    로컬(/opt/atrsite/backups) 최신 2개만 보관됨

--------------------------------------------------------------------
[VPS 실전 배포 -- RackNerd, systemd 서비스 + nginx까지 완료] (2026-08-02)
--------------------------------------------------------------------
  - 사용자가 RackNerd에서 VPS 구매 (2GB KVM, Ubuntu 24.04, 107.173.91.254,
    New York) -- "서버 관련 서비스 다 등록시키고, 코드까지 다 업로드하고
    대시보드 정상작동까지 다 진행하라"는 지시로 전체 배포 진행.
  - root 비밀번호는 채팅으로 안 받음(API 키와 같은 원칙) -- SSH 키 쌍을
    직접 생성해서 공개키만 사용자가 NerdVM 웹 콘솔(VNC)로 root 로그인해
    등록하게 함. 이후 root 비밀번호 없이 키로만 접속.
  - 기본 보안: 시스템 업데이트(361개 패키지) -> strpro sudo 계정 생성
    (NOPASSWD, SSH 키 접속) -> ufw 방화벽(22/80/443만 허용) -> root 직접
    SSH 로그인 차단(PermitRootLogin no, PasswordAuthentication no) ->
    strpro 키로 여전히 접속되고 root는 막힌 것 확인.
  - 로컬에 커밋만 돼 있고 GitHub(bestakas-crypto/atrpro)에 안 올라가 있던
    커밋 4개(QQQ 버그 수정, 해외 TR, strpro.bat, ATR 백필+MODP+HTTP 에러
    수정)를 먼저 push.
  - 배포: python3.12-venv/nginx 설치 -> /opt/atrsite에 git clone -> venv +
    requirements 설치 -> 서버에서도 pytest 140개 전부 통과 확인 -> .env는
    .env.example에서 시작(KIS/텔레그램 값은 비워둠, 사용자가 직접 채워야
    함 -- 이번에도 credential은 내가 직접 안 넣음) -> API_KEY는 내가
    openssl로 랜덤 생성해서 채움(제3자 금융 자격증명이 아니라 이 앱
    자체의 접근 인증용이라 직접 생성 -- 인터넷에 노출되는 대시보드라
    비워두면 아무나 접근 가능해서 위험) -> KIS_IS_PAPER_TRADING=false로
    설정(로컬과 동일하게 실전 계좌 기준).
  - atrsite 시스템 계정 생성(웹/워커 프로세스 전용, 최소 권한) -> systemd
    유닛 4개(web/worker/backup.service/backup.timer) 설치 및 기동 -> 전부
    active 확인.
  - **실제로 재현된 배포 특유의 버그 하나 발견**: 서버 시스템 시간대가
    기본 UTC였음. market_schedule.py의 장중 판정 로직 전체가
    "datetime.now()가 KST"라고 가정하는 구조라(로컬 개발 PC는 이미 KST라
    문제가 없었음), UTC 서버에서는 장중 판정이 9시간 밀려서 완전히
    틀어질 뻔했음 -- `timedatectl set-timezone Asia/Seoul`로 서버
    시스템 시간대 자체를 한국시간으로 바꿔서 해결(코드 수정 아님, 배포
    환경 설정 문제). 이걸 안 고쳤으면 backup.timer도 16:30 KST가 아니라
    16:30 UTC(=한국시간 새벽 1시반)에 돌 뻔했음.
  - nginx를 80번 포트 리버스 프록시로 세팅(127.0.0.1:8000 -> 외부 80) --
    스펙 14.1 "동일 출처" 원칙대로 프런트엔드 정적 파일도 같은 FastAPI가
    서빙하므로 nginx는 그냥 앞단 역할만 함.
  - journald 로그 상한 200M 설정(DEPLOY.md 권장사항).
  - 검증: 공인 IP(http://107.173.91.254)로 대시보드 로딩, API_KEY 없이는
    401, 있으면 200, 종목 생성/삭제(POST/DELETE) 실제 DB 반영까지 브라우저
    기반으로 전부 확인.
  - **남은 유일한 수동 작업**: `.env`의 KIS_APP_KEY/SECRET/ACCOUNT_NO,
    TELEGRAM_BOT_TOKEN/CHAT_ID -- 사용자가 서버에 직접 SSH로 들어가서
    채워야 함(`ssh -i ~/.ssh/strpro_vps strpro@107.173.91.254`, 이후
    `sudo nano /opt/atrsite/.env`, 저장 후
    `sudo systemctl restart atrsite-web atrsite-worker`). 그 전까지는
    더미 모드로 동작(실제 KIS/텔레그램 연동 없음).
  - (이후 완료) .env 실제 값 반영: vi를 30년간 안 써봤다며 로컬에서 작성 후
    업로드하는 방법을 요청 -- 로컬 스크래치패드에 API_KEY/KIS_IS_PAPER_TRADING만
    미리 채운 템플릿을 만들어 메모장으로 편집하게 하고, scp로 서버에
    업로드 -> 파일 이동/권한 설정만 하고 값은 한 번도 출력하지 않음(길이만
    확인) -> 서비스 재시작. 실제 KIS/텔레그램 연동 켜짐 확인.
  - 예전 ATRsite(atrsite.vercel.app, C:\atrsite)에 입력해둔 실제 보유
    데이터를 옮기려는데 모바일에서만 보이고 PC에서 빈 화면이라는 문의 --
    버그가 아니라 localStorage가 기기별로 독립적이라 생기는 구조적 한계
    (바로 이게 서버형으로 새로 만든 이유). C:\atrsite에 "데이터 내보내기"
    버튼을 새로 추가(stock-portfolio/cash-deposits/fx-rates를 JSON으로
    다운로드, strpro의 "기존 데이터 가져오기"가 그대로 읽는 형식) --
    로컬에서 더미 데이터로 클릭 테스트해서 정상 동작 확인. 실제 배포
    (push)는 라이브로 쓰고 있는 앱이라 사용자 확인 후 진행하기로 함.

--------------------------------------------------------------------
[대시보드 통화 버튼 TOTAL/KRW/USD로 개편 (JPY 제거)] (2026-08-02)
--------------------------------------------------------------------
  - 실사용 중 사용자가 모바일 화면(107.173.91.254)을 보면서 버튼 배치를
    요청: 1번째 TOTAL(모든 통화 자산을 원화로 환산한 종합 합계),
    2번째 KRW(원화 종목만, 환산 없이), 3번째 USD(달러 종목만, 환산 없이).
    JPY는 버튼에서 빼고, 일본 투자가 생기면 그냥 TOTAL 환산에 포함되는
    걸로 충분하다고 함(2~3년에 한 번 있을까 말까).
  - 기존 KRW/USD/JPY 버튼은 사실 셋 다 "그 통화로 전부 환산한 합계"였음
    (display_currency를 서버에 저장해서 모든 종목을 그 통화로 변환 -- 즉
    지금의 KRW 버튼이 이미 "TOTAL"과 동일한 개념이었음). 그래서:
    - TOTAL: 기존 로직 그대로 재사용, 다만 타겟 통화를 항상 KRW로
      고정(서버의 display_currency를 항상 'KRW'로만 보냄, 다른 통화
      선택 UI 자체를 없앰)
    - KRW/USD: 완전히 새 로직 -- 서버 호출 없이 클라이언트에서
      dashboard.instruments/deposits를 그 통화(instrument.currency)로
      필터링해서 환산 없이 그대로 합산(dashboard.js의
      computeNativeTotals()). 서버 API 변경 전혀 없음(순수 프런트엔드
      변경) -- 각 종목의 원본 통화 금액은 이미 응답에 들어있었음.
    - 예전 3버튼 시절 저장된 display_currency가 KRW가 아닐 수 있어서,
      TOTAL 모드로 로드될 때 KRW가 아니면 자동으로 KRW로 재설정하는
      자가교정 로직 추가.
  - 로컬에서 KRW종목(매입 1,000)/USD종목(매입 50, 환율 1,300원) 테스트
    데이터로 세 버튼 모두 검증: TOTAL은 기존 실제 데이터+테스트 데이터
    합산 환산값, KRW는 정확히 2,141,000(=기존 kodex200 2,140,000 +
    테스트 1,000, USD 종목 전부 제외), USD는 정확히 13,650(=기존 QQQ
    13,600 + 테스트 50, KRW 종목 전부 제외) -- 필터링 정확히 동작 확인.
  - service-worker.js 캐시 v4 -> v5.
  - pytest는 백엔드 무변경이라 재실행만(140개 그대로 통과) -- 이번 건
    100% 프런트엔드 변경.
  - **실사용 중 "TOTAL이 KRW랑 똑같다" 신고 -> 원인 규명**: 코드 버그가
    아니라 VPS의 fx_rates 테이블에 USD 환율이 아예 없었음(KRW=1.0 고정값
    말고는 아무 것도 저장 안 된 상태) -- QQQ(USD)가 "환율 정보 없음"으로
    TOTAL 계산에서도 조용히 제외되면서 KRW 필터 결과와 우연히 같아
    보였던 것. frankfurter.dev에서 USD/KRW 환율(1449.28)을 받아 PUT
    /api/v1/fx로 직접 채워넣어 해결(코드 수정 없음). 이후 TOTAL이
    481,293,576원(Kodex 100,532,126 + QQQ 환산 380,761,450)으로 KRW
    단독(100,532,126)/USD 단독(262,725)과 확실히 다르게 계산되는 것 확인.
  - 서버 DB에 남은 테스트 데이터 여부도 같이 확인 요청받음 -- 확인 결과
    VPS에는 사용자가 실제로 입력한 QQQ/Kodex 200 두 종목만 있고, 이전에
    검증용으로 만들었던 테스트 종목(__DEPLOY_TEST__ 등)은 전부 그때그때
    바로 삭제해서 남아있지 않음.
  - C:\atrsite(예전 클라이언트 PWA)에 추가해둔 "데이터 내보내기" 버튼도
    이번에 함께 push -> Vercel 자동 배포 확인(atrsite.vercel.app 라이브
    반영, app.js에 btn-export-data 코드 존재 확인).

--------------------------------------------------------------------
[인쇄/PDF용 보고서 화면 추가 (헤더의 새로고침 옆 📄 버튼)] (2026-08-02)
--------------------------------------------------------------------
  - 사용자 요청: 집/사무실에서 프린팅할 수 있는 보고서 UI. 먼저 구조를
    제안(생성 방식은 서버 PDF 라이브러리 없이 브라우저 인쇄 기능 활용 --
    "인쇄용 보기" 화면 -> window.print() -> 인쇄 대화상자에서 실제
    프린터로 뽑거나 "PDF로 저장" 그대로 사용)했고, 상세 수준을 확인한 뒤
    (처음엔 "거래이력까지" -> 곧바로 "거래이력 말고 ATR까지"로 축소)
    구현.
  - frontend/report.html + js/report.js + css/report.css -- 메인 SPA
    (main.js)와 완전히 분리된 독립 페이지. 같은 오리진이라
    api-client.js의 localStorage API 키를 그대로 재사용함. 항상 라이트
    테마 고정(프린터/PDF는 화면 그대로 나가므로 다크 테마 쓰면 안 됨).
  - 보고서 구성: 표지(생성일시+매매판단용 아님 고지) -> 종합 자산 요약
    (TOTAL/KRW/USD 3분할 카드, dashboard.js의 computeNativeTotals를
    export해서 재사용) -> 보유 종목 요약 표 -> 주의가 필요한 신호(BUY/
    TAKE_PROFIT/STOP/SELL_BOTH 트리거된 종목만 강조) -> 종목별 매매
    기준(ATR/다음매수가/익절가/추적손절선/보유후최고가) -> 예금 현황 ->
    각주. 거래이력은 최종적으로 범위에서 뺌.
  - 백엔드 변경 1줄: build_dashboard()의 종목별 행에 atr을 안 넣고
    있었음(instrument_snapshot()에는 있었는데 빠뜨렸던 부분) -- 보고서가
    종목별 ATR을 보여주려면 필요해서 추가. dashboard 응답에 필드 하나
    늘어나는 것뿐이라 기존 프런트엔드/테스트 전혀 안 건드림.
  - 헤더의 "새로고침" 아이콘 버튼 옆에 "보고서"(📄) 버튼 추가. 처음엔
    <button>+window.open()으로 만들었는데 자동화 브라우저(및 일부 모바일
    브라우저)에서 팝업이 조용히 막히는 걸 발견 -- 진짜 <a href="report.html"
    target="_blank">로 바꿔서 팝업 차단 이슈 자체를 없앰(브라우저 네이티브
    새 탭 열기라 JS 권한과 무관).
  - 로컬에서 ATR 채우기/손절 신호 트리거 등 실제 상태 변화를 만들어서
    각 섹션(요약 카드, 종목표, 신호 강조, 매매기준표)이 정확한 값으로
    렌더링되는 것 확인.
  - tests/test_api.py: dashboard 응답에 atr 필드가 종목별로 포함되는지
    검증하는 테스트 1개 추가.
  - service-worker.js 캐시 v5 -> v6.
  - pytest 140 -> 141개, 전부 통과.

--------------------------------------------------------------------
[종목명 옆에 현재가 + 전일 대비 등락률 표시] (2026-08-03)
--------------------------------------------------------------------
  - 사용자 요청: 종목명 옆에 현재가와 어제 대비 등락률 표시("실시간 아니어도
    됨, 최근 갱신된 값만 보여주면 됨" -- 그래서 KIS가 마지막 조회 시점에
    준 값을 그대로 저장/표시하는 방식으로 감).
  - 필드 조사(manual 폴더 명세 직접 확인): 국내 TR([v1_국내주식-008])은
    output.prdy_ctrt(전일 대비율)를 그대로 제공. 해외 TR(price-detail,
    [v1_해외주식-029])은 등락률 필드가 따로 없고 output.base(전일종가)만
    있어서 (현재가-전일종가)/전일종가*100로 직접 계산.
  - kis_client.py: Quote 데이터클래스에 change_pct 필드 추가,
    _get_domestic_price/_get_overseas_price/_dummy_quote 각각 채우도록
    수정. 파싱 실패해도 가격 조회 자체를 막지 않고 None으로만 둠(부수적인
    값이라서).
  - **스키마 변경 시 실수하기 쉬운 부분 처리**: quote_latest 테이블에
    change_pct 컬럼을 추가했는데, CREATE TABLE IF NOT EXISTS는 이미
    데이터가 든 기존 DB(VPS에 실제 QQQ/Kodex 200 데이터가 있음)에는
    컬럼을 안 만들어준다 -- db.py에 _add_column_if_missing() 마이그레이션
    헬퍼를 추가해서 init_db() 안에서 PRAGMA table_info로 확인 후 없으면
    ALTER TABLE로 보충하도록 함. 기존 행 데이터 안 날아가는지 별도
    테스트(tests/test_db.py)로 검증.
  - market_data_repo.upsert_quote/get_quote, portfolio_service.commit_quote,
    worker.py/refresh_quote_now 호출부까지 change_pct를 끝까지 관통시킴.
  - 프런트: 종목 목록 카드의 종목명 아래에 "현재가 (등락률)" 한 줄 추가
    (.stock-card-name-wrap로 세로 배치, 기존 손익률 배지는 그대로 유지 --
    손익률은 매입가 대비, 등락률은 전일 대비로 서로 다른 지표라 둘 다
    남김). 보고서 표에도 "전일 대비" 컬럼 추가.
  - **디버깅 에피소드**: 로컬에서 "주가 갱신"을 눌렀더니 change_pct가
    계속 null로 나와서 원인을 팠는데, 코드 버그가 아니라 오늘 테스트를
    너무 많이 해서 KIS 토큰 발급이 다시 403으로 막혀 있었던 것(기존에
    성공했던 옛날 시세가 그대로 남아있었을 뿐, 실제로는 갱신 자체가
    조용히 실패하고 있었음 -- 에러 핸들링은 정확히 의도대로 동작). 직접
    _get_domestic_price() 단위 호출로 파싱 로직 자체는 정상(24.17%)임을
    별도로 확인했고, 프런트 렌더링은 DB에 값을 수동으로 넣어서 검증함.
  - tests/test_kis_client.py: 국내 prdy_ctrt 파싱, 해외 base 기반 계산,
    필드 없을 때 None 폴백 -- 테스트 3개 추가.
  - service-worker.js 캐시 v6 -> v7.
  - pytest 141 -> 143개, 전부 통과.

--------------------------------------------------------------------
[헤더 레이아웃 수정 -- 보고서 버튼 오른쪽으로, 타이틀 다시 정중앙] (2026-08-03)
--------------------------------------------------------------------
  - 실사용 스크린샷(모바일)에 화살표로 표시된 피드백: 보고서(📄) 버튼을
    새로고침 옆(왼쪽 내비게이션 묶음)에서 오른쪽 끝으로 옮기고, 타이틀
    "ATRsite-pro"는 다시 화면 정중앙에 오게 해달라는 요청.
  - 원래 .app-header는 단순 flex row라서 왼쪽 아이콘 묶음 다음에 타이틀이
    바로 따라붙는 구조였음(애초에 "진짜 중앙 정렬"이 아니었고, 아이콘이
    3개일 땐 우연히 중앙과 비슷해 보였을 뿐 -- 보고서 버튼이 추가돼
    4개가 되면서 비대칭이 눈에 띄게 됨).
  - index.html: 헤더를 3개 구역으로 분리 -- .header-nav(왼쪽,
    뒤로/앞으로/새로고침) / h1(가운데) / .header-actions(오른쪽, 보고서
    버튼만).
  - style.css: .app-header를 flex에서 grid-template-columns: 1fr auto 1fr
    로 변경 -- 좌우 구역 너비가 달라도 가운데 컬럼(타이틀)이 항상 정확히
    중앙에 오도록 함(justify-self: start/center/end 조합).
  - 모바일 뷰포트(375px)로 좌표 직접 측정해서 검증: 타이틀 중심이 헤더
    중심에서 4px밖에 안 벗어남(사실상 중앙), 보고서 버튼이 오른쪽 끝
    (359px 지점)에 위치.
  - service-worker.js 캐시 v7 -> v8.
  - pytest 143개 그대로 통과(순수 레이아웃 변경이라 백엔드 무관).

--------------------------------------------------------------------
[LLM 매크로 브리핑 기능 -- 헤더 아이콘 하나로 오늘 시황 브리핑] (2026-08-03)
--------------------------------------------------------------------
  - 사용자 요청: "운전/이동 중에도 터치 한 번으로 그날의 매크로 수치·뉴스·
    유념사항을 들을 수 있게" -- 별도 요청/응답 방식(자동 호출 없음).
    C:\mmean의 LLM API 제반사항을 참고하되 직접 import는 안 함(두 프로젝트
    독립 유지).
  - 핵심 원칙: LLM이 수치를 스스로 찾거나 추측하지 않는다 -- Python이
    검증된 수치(보유종목 현재가/등락률, 원달러 환율, VXN)를 먼저 모으고
    LLM은 설명/요약/시나리오만 담당.
  - adapters/llm_client.py 신규: Claude/GPT/Gemini/DeepSeek 4개 공급자,
    ask(system, user, *, use_web_search, chain)로 호출. chain은 provider
    이름이 아니라 "단계 이름"으로 키를 잡음(stage1_search/stage2_judgment)
    -- 처음엔 provider 이름으로 키를 잡았다가, 1단계/2단계가 둘 다
    Gemini로 시작하게 되면서(비용 문제로 클로드를 최후 폴백으로 내림)
    같은 키로 다른 체인을 표현 못 하는 문제가 생겨 리팩터링함.
    stage1_search(객관조회): gemini->gpt->claude. stage2_judgment(판단):
    gemini->gpt->deepseek->claude(클로드가 가장 비싸서 최후 폴백).
    DeepSeek는 자체 웹서치 도구가 없어서(api-docs.deepseek.com 확인)
    1단계엔 못 씀, 2단계 저렴한 폴백 자리만.
  - adapters/market_index_client.py: Yahoo Finance 비공식 엔드포인트로
    VXN 등 지수 조회, 실패해도 절대 예외 안 던지고 status=UNAVAILABLE로.
  - services/analysis_service.py: VXN 그리드 매수 규칙(사용자의 실제
    투자법 -- 0~15 IQQ10주/15~24 QQQM1주/25~30 QQQ1주/30~40 상황체크,
    24~25 틈과 40 이상은 정의 안 된 채로 UNDEFINED 유지, 임의로 안 채움)
    을 Python이 확정하고 LLM은 재계산 없이 그대로 인용하도록 프롬프트에
    명시. 2단계 파이프라인(1단계 Gemini 우선 웹서치로 SOXX/MU/금리/환율
    등 객관조회 -> 2단계가 스냅샷+1단계 결과로 해석/시나리오 작성),
    5분 캐시로 중복 LLM 호출 방지.
  - schema.py: analysis_results 테이블(id/snapshot_json/result_text/
    provider/model) 신규.
  - frontend: briefing.html/js/css 신규(report.html과 같은 독립 페이지
    패턴), 헤더에 🌐 아이콘으로 진입(처음엔 목록 화면에 큰 버튼으로
    만들었다가, 사용자가 실기기 스크린샷 보고 "그렇게 크게 하지 말고
    아이콘 형태로" 정정 요청해서 헤더 아이콘으로 이동).
  - 실키 라이브 검증 중 발견해서 고친 실제 버그 3건: (1) Gemini 기본
    모델명이 신규 사용자에게 지원 종료된 구버전(gemini-1.5-flash) --
    ListModels로 확인해서 gemini-flash-latest로 교체 (2) [보안] Gemini
    키가 URL 쿼리스트링(?key=)에 있어서 요청 실패시 uvicorn.log에 키가
    평문으로 남는 걸 실제로 확인(tail로 직접 봄) -- x-goog-api-key
    헤더 방식으로 변경, 이미 찍힌 로그는 redact (3) Claude 웹서치 호출이
    max_tokens=2000에 걸려 검색만 하고 텍스트 없이 잘림(stop_reason=
    max_tokens로 재현) -- 웹서치 호출만 8000으로 증량.
  - 키 4개(ANTHROPIC/OPENAI/GEMINI/DEEPSEEK) 전부 로컬 .env에 사용자가
    직접 입력(메모장 템플릿 방식, 값은 한 번도 직접 안 읽음)하고 값
    확인 없이 존재/길이만 확인하는 스크립트로 병합.
  - pytest 143 -> 176개, 전부 통과.
  - 커밋(27aebb2) 후 원격 push + VPS 배포까지 완료: git pull(atrsite
    소유 repo라 strpro 계정에서 dubious ownership 에러 -> safe.directory
    1회 등록 후 sudo -u atrsite git pull), LLM 키 4개는 로컬 .env에서
    값을 안 읽고 파이프로 원격 append, atrsite-web 서비스만 재시작
    (analysis_results는 CREATE TABLE IF NOT EXISTS라 자동 마이그레이션).
    실서버(http://107.173.91.254)에서 헤더 🌐 아이콘 실제 노출 확인.
  - 상세 작업 로그: WORK_LOG_llm_briefing.md (귀가 후 3개 질문에 대한
    답변과 그 반영 과정까지 포함, 아직 별도 커밋 안 됨).

====================================================================
[종목탐구(company-explorer) 기능 -- 작업 시작] (2026-08-03)
====================================================================
사용자가 4시간 외출하며 대형 스펙(20절)을 전달하고 "질문하지 말고 너의
추천사항으로 진행, 곤란한 것만 귀가 후 물어보라"고 위임함. 음성 기능은
스펙과 별개로 이번엔 포기(사용자 명시). 아래는 코드 수정 전 필수 확인
사항(스펙 1절) 보고 -- 승인 대기 없이 바로 이어서 구현 진행.

1. 현재 메뉴/라우트 구조
--------------------------------------------------------------------
- index.html(SPA)은 진짜 해시 라우팅(#list, #detail/<id>)을 씀 --
  main.js가 history.pushState + popstate로 view-list/view-detail
  섹션을 토글. 이 라우터는 "같은 페이지 안"에서만 동작.
- report.html/briefing.html은 이 해시 라우터에 안 엮여 있음 -- 완전히
  독립된 별도 HTML 페이지(자체 <script type=module>, api-client.js만
  재사용해서 같은 오리진 인증 공유). 헤더의 아이콘 링크
  (<a target=_blank>)로 진입, 새 탭에서 열림.
- 헤더(.app-header)는 grid(1fr auto 1fr) 3구역: .header-nav(왼쪽,
  뒤로/앞으로/새로고침 3개) / h1(가운데, "ATRsite-pro" 고정) /
  .header-actions(오른쪽, 아이콘 링크들).
- 새로고침(btn-nav-refresh)은 현재 location.reload()(전체 새로고침) --
  스펙 3.4가 요구하는 "화면별 맞는 재조회"는 아직 아님. main.js에
  refreshCurrentView()라는 헬퍼가 이미 있는데(현재 뷰가 detail이면
  loadAndRenderDetail, 아니면 loadAndRenderList) 새로고침 버튼에 연결이
  안 돼 있었음 -- 이번에 연결함(아래 3번 참고).
- 판단: report.html/briefing.html처럼 종목탐구도 독립 페이지
  (company-explorer.html)로 만드는 게 이 프로젝트의 기존 관례와 일치.
  index.html의 내부 해시 라우터에 억지로 엮으면(다른 2개 기능과
  다르게) 오히려 일관성이 깨짐 -- 스펙 3.3의 "#company-explorer"는
  예시일 뿐이고 "현재 프로젝트의 해시 라우팅 형식을 따르라"는 단서가
  있어 이 판단을 따름.

2. 현재 매크로 LLM 모듈 구조
--------------------------------------------------------------------
- adapters/llm_client.py: is_dummy_mode()/ask(system, user, *,
  use_web_search, chain)/LLMResponse/LLMError. chain은 provider 이름이
  아니라 "stage1_search"/"stage2_judgment" 같은 역할 이름 -- 여러 기능이
  같은 provider로 시작하는 서로 다른 체인을 가질 수 있게 일반화돼 있음.
  종목탐구도 이 두 체인을 그대로 재사용 가능(1단계=최신 공시/뉴스
  객관조회, 2단계=Python이 계산한 재무지표로 서술/판단).
- adapters/market_index_client.py: 외부 지수 조회 공통 패턴(실패해도
  절대 예외 안 던지고 status=UNAVAILABLE) -- 새 외부 데이터 어댑터
  (SEC EDGAR/OpenDART)도 이 원칙을 그대로 따름.
- services/analysis_service.py: (a) Python이 먼저 확정 수치를 계산 ->
  (b) 그 결과를 "재계산하지 말고 그대로 설명만 하라"고 프롬프트에
  명시 -> (c) 5분 캐시로 중복 LLM 호출 방지. 종목탐구도 동일 골격
  재사용(재무지표는 Python 확정, LLM은 서술만).
- schema.py: CREATE TABLE IF NOT EXISTS만 쓰는 무중단 마이그레이션
  패턴(기존 데이터 보존, db.py의 _add_column_if_missing()도 있음).
- api/analysis.py + repositories/analysis.py: 스펙 12절이 요구하는
  요청ID/폴링 패턴은 아직 없음(macro는 동기 처리, 개인용 저빈도라
  충분했음) -- 종목탐구는 SEC/OpenDART 실제 API 호출이 여러 번 겹쳐서
  더 오래 걸릴 수 있으므로 이번엔 요청ID+상태 폴링을 실제로 도입함
  (스펙 12절 그대로).

3. 재사용 가능한 MMEAN 패턴 (C:\mmean\llm\llm_chain.py 확인함, 복사/
   import 안 하고 개념만 재작성)
--------------------------------------------------------------------
- JSON 추출/정규화(코드펜스 제거, 중괄호 균형 파싱, Python 리터럴->JSON):
  이번 기능엔 불필요 -- 스펙 9.3의 LLM 출력은 macro 브리핑과 동일하게
  20절 구조의 "서술문"이고, IMPROVING/STABLE/DETERIORATING 같은 상태값은
  스펙 6.3에 따라 Python 계산 엔진이 만들어 DB에 저장하는 것이지 LLM이
  JSON으로 반환하는 게 아님. 그래서 JSON 스키마 검증/추출 로직은 이번엔
  가져오지 않음(불필요한 복잡도 추가 방지).
- 토큰/비용/지연시간 로깅(attempts 리스트, latency_ms, provider,
  model, fallback_depth): 스펙 14절의 llm_usage 요구사항과 정확히
  대응 -- 이 개념은 채택. 다만 mmean처럼 일일 호출/토큰 한도(daily
  limit)는 macro 기능에도 없었고 개인 저빈도 사용이라 이번에도 생략
  (과설계 방지, 프로젝트 기존 판단 기조와 일치).
- 공급자별 재시도+백오프(2s/4s/8s, 타임아웃/네트워크 오류에만):
  기존 llm_client.py는 재시도 없이 즉시 다음 공급자로 폴백하는 더
  단순한 방식이고 macro 기능에서 이미 실전 검증됨 -- 이번에도 동일
  방식 유지(공용 llm_client.py를 고치면 macro 기능 동작까지 바뀌므로
  안전한 것만 그대로 씀). 재시도 로직 자체를 새로 추가하진 않음.
- 결론: llm_client.py는 그대로 재사용(수정 없음), 새 LLM 클라이언트는
  안 만듦(스펙 요구사항 그대로 준수).

4. 추가/수정할 파일 목록 (MVP 범위 -- 아래 "이번 4시간 범위" 참고)
--------------------------------------------------------------------
신규 백엔드:
  backend/atrsite/adapters/sec_edgar_client.py   (SEC EDGAR, 키 불필요)
  backend/atrsite/adapters/opendart_client.py    (OpenDART, 키 필요 --
                                                    더미 모드로 골격만)
  backend/atrsite/services/company_metrics_engine.py (정량 계산 + 경고규칙)
  backend/atrsite/services/company_analysis_service.py (스냅샷 조합 +
                                                    2단계 LLM 파이프라인)
  backend/atrsite/repositories/companies.py
  backend/atrsite/repositories/company_analysis.py
  backend/atrsite/api/company.py
신규 프런트엔드:
  frontend/company-explorer.html / css/company-explorer.css /
    js/company-explorer.js
수정:
  frontend/index.html(헤더에 🔍 종목탐구 아이콘, 순서를 보고서/종목탐구/
    오늘 브리핑으로 재배치), frontend/js/main.js(새로고침 버튼을
    refreshCurrentView()에 연결), frontend/js/api-client.js(신규
    엔드포인트), backend/atrsite/schema.py(신규 테이블),
    backend/atrsite/main.py(신규 라우터 등록), service-worker.js
    캐시 버전.

5. DB 마이그레이션 계획
--------------------------------------------------------------------
- 별도 DB(data/analysis.db)로 분리하는 스펙 13절 제안은 채택 안 함 --
  기존 analysis_results 테이블도 이미 같은 atrsite.db에 있고, SQLite
  파일 하나로 계속 가는 게(크로스 DB 조인 불필요, 백업 스크립트가 이미
  단일 파일 기준으로 동작) 이 프로젝트 규모에 더 단순하고 안전하다고
  판단(과설계 방지 원칙).
- 전부 CREATE TABLE IF NOT EXISTS(기존 무중단 패턴), 이름 충돌 방지로
  전부 company_ 접두사:
  companies, company_identifiers, company_filings,
  company_financial_periods, company_financial_metrics,
  company_news, company_snapshots, company_analysis_requests,
  company_analysis_results.
- 스펙의 industry_metrics/llm_usage/audio_files 3개는 이번 MVP에서
  별도 테이블로 안 만듦: industry_metrics는 company_financial_metrics에
  metric_key 네임스페이스로 흡수(테이블 수 최소화), llm_usage는 나중에
  실사용 패턴을 보고 필요하면 추가(지금은 company_analysis_results에
  provider/model/latency_ms만 우선 기록), audio_files는 음성 기능 자체를
  포기했으므로 불필요.
- 기존 사용자 데이터(거래/보유종목) 테이블은 전혀 건드리지 않음.

6. 단계별 테스트 계획
--------------------------------------------------------------------
- sec_edgar_client: 실제 공개 API로 라이브 테스트(키 불필요) + 네트워크
  실패/필드 누락 시 UNAVAILABLE 폴백 테스트
- opendart_client: 키 없는 더미 모드 테스트만(실제 키가 없어서 라이브
  검증은 귀가 후 키를 받아야 가능 -- 아래 "귀가 후 확인" 참고)
- company_metrics_engine: 성장률/마진/FCF/현금전환율/순차입금/
  이자보상배율 계산 + 경고 규칙(매출채권>매출 증가율 등) 단위 테스트,
  기존 atr_engine.py 테스트 스타일(고정 픽스처로 정확한 기대값 검증)
- company_analysis_service: 캐시/폴링 상태 전이/더미모드 -- 기존
  analysis_service 테스트 패턴 그대로
- api/company.py: FastAPI TestClient로 엔드포인트별 인증/상태코드
- 회귀 방지: 기존 176개 테스트가 계속 통과하는지 매 체크포인트마다 확인

이번 4시간 범위(현실적 판단, 스펙 20절 전체를 다 채우진 못함):
- 미국 기업(SEC EDGAR, 키 불필요)을 실데이터로 완전히 동작하게 만드는
  것을 최우선으로 함(검증 가능하니까). 한국 기업(OpenDART)은 API 키가
  있어야 회사코드 매핑 자체를 못 받아오므로, 어댑터 골격은 만들되
  더미/미가용 모드로 두고 "귀가 후 확인" 항목으로 넘김.
- 업종 템플릿은 스펙이 1순위로 지정한 반도체만 구현, 나머지는
  INSUFFICIENT_DATA로 명확히 표시.
- 스펙 12절 폴링 상태(PENDING~COMPLETED)는 만들되, GENERATING_AUDIO는
  음성 포기로 제외.
- 커밋은 로컬까지만(스펙 19절 명시), push/배포 안 함.

====================================================================
[종목탐구 -- 1차 구현 완료(미국 기업), 실데이터 라이브 검증 완료]
====================================================================
2026-08-03, 위 위임 지시 이후 이어서 작업. 4시간 범위에서 "미국 기업
우선"으로 좁힌 MVP가 완전히 동작하는 상태로 끝남 -- 실제 SEC 데이터로
Micron/Microsoft/QQQ 3개 종목을 브라우저에서 직접 검색->확인->분석까지
전부 실행해서 확인함(스펙 7단계 Shadow 검증의 축소판).

구현 내역
--------------------------------------------------------------------
- 헤더: [보고서][종목탐구🔍][오늘 브리핑] 순서, 새로고침 버튼을
  refreshCurrentView()에 연결(전체 새로고침 -> 화면별 재조회)
- DB: companies/company_identifiers/company_filings/
  company_financial_periods/company_financial_metrics/
  company_analysis_requests/company_analysis_results 7개 테이블(스펙
  9개에서 industry_metrics는 financial_metrics에 흡수, company_news/
  company_snapshots는 매크로 브리핑처럼 snapshot_json에 흡수해서 축소).
  같은 atrsite.db 안에(별도 DB 분리 안 함, 과설계 방지)
- adapters/sec_edgar_client.py: company_tickers.json 검색 + companyfacts
  XBRL 정규화(최근 8개 분기/4개 연간). 키 불필요, User-Agent만 필요
  (SEC_USER_AGENT 환경변수, 기본값은 플레이스홀더 -- 실사용량 늘면 실제
  연락처로 채우길 권장)
- adapters/opendart_client.py: 골격만(키 없어서 실제 파싱 미검증,
  실행하면 명확한 에러 -- "추측으로 구현 안 함" 원칙)
- services/company_metrics_engine.py: 마진/FCF/순차입금/이자보상배율/
  현금전환율 계산, IMPROVING~INSUFFICIENT_DATA 판정, 경고 규칙 6종
  (스펙 9종 중 단기차입금/CAPEX가동률/일회성이익 3종은 데이터 자체가
  없어서 미구현 -- 임의 추정 안 함), 반도체 재고일수(inventory_days)
  전용 계산
- services/company_analysis_service.py: 2단계 LLM(1단계=stage1_search,
  2단계=stage2_judgment -- 매크로 브리핑과 동일한 체인 재사용, 새 LLM
  클라이언트 안 만듦), 20항목 구조 프롬프트, ETF/한국기업 명확히 차단
- api/company.py: 스펙 12절 그대로 -- POST /company-analysis/run이
  BackgroundTasks로 즉시 202+요청ID 반환, GET .../{id}로 폴링. 백그라운드
  작업은 요청-스코프 커넥션을 공유하지 않고 별도 connect()(worker.py와
  동일 원칙)
- frontend/company-explorer.html+js+css: 검색->기업확인->진행상태 폴링
  ->한눈에보기(뱃지)+경고+20항목 서술 결과. report.html/briefing.html과
  동일한 독립 페이지 패턴
- 테스트 87개 신규(companies_repo 9, sec_edgar_client 7, opendart_client
  3, company_metrics_engine 13, company_analysis_service 9, company API
  6 등) + 기존 143개 유지, 전체 226개 통과 (2026-08-03 기준)

라이브 검증 중 발견해서 고친 실제 버그 3건
--------------------------------------------------------------------
1. SEC 원본 데이터의 fy/fp 태그가 비교(전년동기) 컬럼에 실제 기간과
   다른 값이 붙는 걸 실제로 확인(서로 다른 두 분기가 둘 다 "2026 Q1"로
   태깅됨) -- DB UNIQUE 제약을 fiscal_year/fiscal_period가 아니라
   period_end(항상 신뢰 가능한 실제 날짜) 기준으로 바꾸고, 라벨 자체도
   해당 회사의 자체 회계연도 표기가 아니라 period_end에서 직접 계산한
   달력 분기로 바꿈(부정확한 걸 정확해 보이게 두는 것보다 낫다고 판단)
2. company-analysis 2단계(20항목) 응답이 MAX_TOKENS=2000(매크로
   브리핑 10항목 기준)에 걸려 3줄 만에 잘리는 걸 실제로 확인 --
   llm_client.ask()에 max_tokens 오버라이드 파라미터를 추가해서 종목탐구
   2단계는 8000으로 지정(공용 클라이언트 자체를 건드리지 않고 호출자가
   필요한 만큼만 늘릴 수 있게 일반화)
3. (버그는 아니지만) "분석 기준일" 항목이 뉴스 날짜와 혼동되는 경향이
   보여서 프롬프트에 "재무 스냅샷의 최근 분기 period_end를 쓰라"고
   명시적으로 추가

실제 확인된 동작(브라우저 라이브 테스트)
--------------------------------------------------------------------
- Micron(반도체 템플릿 자동 감지) 분석: 20항목 전부 생성, IMPROVING/
  INSUFFICIENT_DATA 판정을 Python 결과 그대로 인용, 목표주가 없으면
  "목표주가 자료 없음"으로 정직하게 표시
- Microsoft 분석: 경고 규칙 2건 실제로 트리거됨(재고 증가율이 매출
  증가율보다 13.1%p 높음 / 발행주식수 증가율이 순이익 증가율보다 높음)
  -- 실제 SEC 데이터로 경고 로직이 의도대로 작동하는 것 확인
- QQQ(ETF) 검색 -> 확인 화면에서 "ETF는 분석 대상 아님" 안내 + 분석
  버튼 비활성화 정상 동작
- "다른 종목 검색" 리셋 후 "최근 분석"에 MU+MSFT 둘 다 정상 표시

이번 범위에서 의도적으로 안 한 것 (귀가 후 확인/결정 필요)
--------------------------------------------------------------------
1. **한국 기업(OpenDART) 실제 분석** -- API 키가 없어서 검색부터 막힘.
   opendart.fss.or.kr에서 회원가입 후 키 발급받아 주면, 기존 LLM 키
   넣던 방식(메모장 템플릿 -> 값 안 읽고 병합)으로 넣고 실제 구현+라이브
   검증 진행 가능. 지금은 명확한 안내 메시지("한국 기업 분석은 아직
   지원하지 않습니다")만 나옴
2. **업종 템플릿** -- 반도체만 구현(스펙 1순위). 은행/SaaS/정유 등은
   INSUFFICIENT_DATA. 필요하면 추가 확장
3. **음성 기능** -- 사용자가 이번엔 명시적으로 포기 지시("음성기능은
   포기한다"), 완전히 제외함
4. **매크로 연동** -- 반도체 업종만 아주 가볍게(최근 매크로 브리핑 존재
   여부만 참고) 연결. 스펙 8절이 원하는 "은행->기준금리, 정유->WTI" 같은
   업종별 세부 매핑은 각 지수를 market_index_client.py에 추가하고 실제
   라이브 검증하는 작업이 더 필요해서(VXN 하나 검증하는 데도 상당한
   시간이 들었음) 이번엔 반도체(VXN)만 하고 확장 안 함
5. **캐시 정책** -- 스펙 14절의 "확정 재무제표는 새 공시 전까지 캐시"는
   DB에 저장은 되지만 재사용(캐시 히트 시 SEC 재호출 스킵) 로직은 아직
   없음 -- "새로 분석" 누르면 항상 SEC를 다시 호출함. 트래픽이 적어서
   당장은 문제 없지만, 필요하면 나중에 추가
6. **llm_usage 테이블** -- 스펙이 요청한 토큰/비용 세부 로깅 테이블은
   안 만듦(company_analysis_results에 provider/model만 기록). 실사용
   패턴 보고 필요하면 추가
7. 재무제표 정정(restatement) 시 과거 분석 결과를 자동으로 재계산하는
   로직 없음(매번 "새로 분석"을 눌러야 최신 정정치 반영)

git: 로컬 커밋까지만 수행(스펙 19절 "원격 push와 배포는 별도 요청시만"
그대로 준수). 이번 세션에서 만든 데이터(MU/MSFT 실제 분석 결과)는 로컬
DB에 실제로 남아있음 -- 귀가 후 바로 확인 가능.

====================================================================
[종목탐구 -- OpenDART 실제 키 반영, 한국 기업(삼성전자) 실데이터 검증 완료]
====================================================================
2026-08-03, 위 "1차 구현" 직후 사용자가 OpenDART 키를 발급받아 옴(메모장
템플릿 방식으로 값 안 읽고 병합, 기존 원칙 그대로). "귀가 후 확인" 1번
항목을 바로 이어서 실제 구현.

OpenDART 실제 API 응답을 라이브로 먼저 확인한 뒤 구현(추측으로 안 짬):
- corpCode.xml: crtfc_key만 있으면 ZIP(11만여 개 법인) 그대로 줌. 삼성전자
  corp_code=00126380 확인. stock_code가 빈 값인 비상장 법인은 검색에서
  제외(투자 가능 종목만).
- fnlttSinglAcntAll.json: sj_div(BS/IS/CIS/CF)별로 계정이 섞여 옴,
  account_id는 IFRS 표준 태그라 회사 바뀌어도 비교적 안정적.
- **핵심 발견(직접 산수로 검증)**: 분기 보고서(11012 반기/11013 1분기/
  11014 3분기)의 손익계산서(IS) 항목은 thstrm_amount가 이미 "그 분기
  단독" 값이고 thstrm_add_amount가 누적값(3분기 누적 240조 - 반기 누적
  154조 = 3분기 thstrm_amount 86조와 정확히 일치하는 것까지 확인).
  반면 현금흐름표(CF) 항목은 thstrm_amount가 항상 "연초부터 누적"값이라
  (thstrm_add_amount 필드 자체가 없음) 분기 단독 값을 얻으려면 구간별로
  직접 빼야 함 -- 연간 총합과 대조해서(Q1~Q4 합 = 연간 총액) 이 해석이
  맞다는 것도 확인.
- 4분기는 DART가 직접 안 줌(사업보고서에 "4분기" 개념이 없음) -- 연간
  총합에서 3분기 누적을 빼서 구함(IS/CF 둘 다).
- 총차입금(total_debt)은 SEC의 LongTermDebt처럼 표준화된 단일 태그가
  없어서(회사마다 차입금 계정 구성이 다름) 매핑 안 함 -- 순차입금은
  INSUFFICIENT_DATA로 정직하게 남김(임의 추정 안 함).

opendart_client.py를 실제 구현으로 교체(기존엔 골격만, 실행하면 명확한
에러만 내던 상태). search_companies/normalize_periods(ANNUAL/QUARTER)
신규. company_analysis_service.py에 resolve_company_from_kr_match +
_collect_kr_periods 추가, build_snapshot이 country로 US(SEC)/KR(DART)
분기하도록 재구성. api/company.py에 POST /company/resolve-kr 신설.
프런트(company-explorer.js)의 selectMatch()가 country로 분기해서 KR도
분석 가능하게 연결.

**라이브 검증 중 실제로 발견해서 고친 버그 2건**:
1. 4분기 IS 값을 처음엔 연간 보고서의 thstrm_amount(연간 총합)를 그대로
   썼는데, 이건 "4분기 단독"이 아니라 "연간 전체" 값이라 완전히 틀림
   -- 직접 만든 단위 테스트가 바로 이 버그를 잡아냄(테스트를 버그에
   맞춰 고치지 않고 코드를 고침). 연간 총합에서 3분기 누적(thstrm_add
   _amount)을 빼도록 수정.
2. 미래 분기(아직 공시 전이라 보고서 자체가 없음)가 전부 None인 채로
   그대로 결과에 끼어들어, 프런트의 "한눈에 보기" 배지가 항상
   "자료부족"으로만 나오던 버그 -- _subtract_cf()가 항상 CF_TAGS 전체
   키를 채운 dict(값은 None이라도)를 돌려줘서 "dict가 비어있는가"로는
   빈 분기를 못 걸렀던 게 원인. "값이 하나라도 실제로 있는가"로 판정
   기준을 바꿔서 해결. 브라우저로 삼성전자를 실제로 다시 분석해서
   배지가 정상적으로 "개선/개선/개선/자료부족"(순차입금만, 정상)으로
   뜨는 것까지 확인.

**실제 확인된 동작(브라우저+API 라이브, 삼성전자 005930)**:
- 검색 -> 확인(대한민국/KRW 표시) -> 분석 -> 20항목 결과까지 전체 흐름
  정상 동작
- 분기별 실제 수치 정합성 검증: 2025년 Q1~Q4 매출 합계(79.14+74.57+
  86.06+93.84=333.61조)가 연간 보고서 총액(333,605,938,000,000원)과
  소수점까지 정확히 일치. 영업현금흐름도 동일하게 검증됨(Q1~Q4 합
  85.31조 = 연간 총액 85,315,148,000,000원과 일치) -- Q4 역산 로직과
  CF 누적 뺄셈 로직 둘 다 실데이터로 완전히 검증됨
- 경고 규칙 실제 트리거 확인: "매출채권 증가율(60.9%)이 매출 증가율
  (42.7%)보다 18.3%p 높음" -- 실데이터 기반 정상 동작
- pytest 237개 전부 통과(신규 8개: OpenDART 파싱/구간뺄셈/미래분기
  스킵 등)

**남은 제약(그대로 유지)**: 순차입금/이자보상배율은 여전히
INSUFFICIENT_DATA(총차입금 매핑 안 함, 위 이유 참고), KR 업종 템플릿
없음(반도체 지표 등 미제공), KR 기업의 ETF 판별은 이름 키워드 휴리스틱
(KODEX/TIGER 등)이라 완벽하지 않음.

git: 이번에도 로컬 커밋까지만.

====================================================================
[종목탐구+매크로 브리핑 실서버 정식 배포 + 배포 직후 버그 2건 수정] (2026-08-03)
====================================================================
- 매크로 브리핑 잘림 수정을 파일 하나만 scp로 올렸던 핫픽스가, 그 파일이
  의존하는 llm_client.py의 max_tokens 파라미터를 실서버가 아직 몰라서
  `TypeError: ask() got an unexpected keyword argument 'max_tokens'`로
  매번 500(Internal Server Error) 발생 -- 사용자 제보("제미나이 배제
  시키고 해봐라")로 알게 됐는데, 실제 원인은 제미나이가 아니라 이 배포
  누락이었음(journalctl로 정확한 트레이스백 확인). 지금까지 로컬에만
  쌓아뒀던 종목탐구 커밋 전체를 정식으로 git push -> 서버 git pull ->
  재시작으로 배포해서 해결(부분 파일 배포의 위험성을 실제로 겪음 --
  이후로는 의존성 있는 수정은 꼭 전체를 커밋 단위로 배포).
- OpenDART 키도 이번에 실서버 .env에 반영(로컬과 동일하게 값 안 읽고
  파이프로 append).
- 배포 직후 두 번째 버그: 종목탐구에서 "검색 실패: api.searchCompany is
  not a function" -- briefing.html/company-explorer.html이 헤더 아이콘
  으로 index.html 안 거치고 바로 새 탭에서 열리는데, 이 두 페이지엔
  서비스워커 갱신 확인 로직이 아예 없어서(index.html의 main.js에만
  있었음) 배포 직후에도 예전 캐시된 api-client.js가 계속 쓰일 수 있었음
  -- main.js와 같은 등록+update()+controllerchange 리로드 로직을 두
  페이지에도 추가, 캐시 v11->v12.
- 사용자 요청으로 브리핑/종목탐구에도 report.html과 같은 인쇄 버튼
  추가(.no-print + @media print, window.print()).
- 배포 확인: 실서버에서 api-client.js/service-worker.js 버전 확인,
  브리핑 max_tokens 함수 시그니처 확인(SSH로 직접 python inspect).
  기존에 열려있던 탭은 한 번은 수동으로 새로고침해야 새 서비스워커
  갱신로직 자체를 받아옴(그 이후부터는 자동).

====================================================================
[OpenDART 검색 504 오류 수정 + LLM 말투 개편("언니 톤") + 음성 듣기 추가]
====================================================================
2026-08-03, 실기기 사용 중 발견된 버그와 요청 3건을 이어서 처리.
- OpenDART corpCode.xml 다운로드가 서버 회선상 실제로 최대 4분 걸려서
  nginx 60초 타임아웃에 504로 실패하던 것 발견/수정 -- 디스크 캐시(24시간)
  + 서버 시작 시 백그라운드 사전예열 + nginx 타임아웃 300초로 증량.
  부수적으로 테스트가 실제 로컬 data/ 디렉터리를 오염시키던 격리 버그도
  같이 고침.
- 사용자가 평소 제미나이에 거는 "여고생 눈높이로 쉽게 설명" 프롬프트를
  종목탐구 -> 매크로 브리핑 순서로 적용. 20개/10개 항목 구조와 수치·판정
  정확성 규칙은 그대로 유지하고 설명 톤만 "친한 언니" 스타일(이모지,
  구어체, 전문용어 괄호 설명)로 변경. 브리핑은 운전 중 청취 제약(문장
  짧게)도 같이 유지.
- 브리핑/종목탐구 결과 화면에 "🔊 듣기" 버튼 추가 -- 서버 TTS 없이 무료
  브라우저 내장 Web Speech API만 사용("일단 나오기만 하면 됨", 예쁜
  목소리는 나중에 LLM 기반으로 업그레이드 예정). 마크다운/이모지 필터링
  후 읽음, 토글로 정지 가능.
- 전부 로컬 검증 후 실서버 배포 완료. pytest 242개 유지.

====================================================================
[v1.0 완료 선언] (2026-08-03)
====================================================================
사용자가 "이제 v1.0으로 완료, 써보다가 필요하면 업그레이드"라고 선언.
현재 포함된 기능: ATR 기반 분할매수/매도 신호(국내+해외 KIS 연동),
예금/통화환산 종합 자산 요약, 인쇄용 보고서, LLM 매크로 브리핑(VXN 그리드
규칙 자동판정), 종목탐구(SEC EDGAR 미국 + OpenDART 한국 재무 분석,
경고 규칙, 20항목 리포트), 전부 "친한 언니" 톤 + 음성 듣기 지원.
RackNerd VPS에 systemd+nginx로 실배포, 모바일에서 실사용 중.

다음 버전(v2) 계획으로 사용자가 언급한 것: **조건부 매매 신호**(예:
"삼성전자가 최고가 대비 10% 떨어지면 KODEX 200을 분할매도"처럼 한 종목의
가격 조건으로 다른 종목의 매매 신호를 트리거하는 크로스종목 규칙).
단, **완전 자동매매는 안 된다는 제약을 사용자가 명확히 함**: 보유 종목이
여러 증권사 계좌에 분산돼 있고, 이 앱이 실제로 자동화 가능한 건 KIS
(한국투자증권) 계좌뿐이라서, KODEX 200 같은 특정 종목을 한 계좌로 몰아
모으지 않는 한 진짜 자동매매는 어렵다고 판단함 -- 그래서 v2도 "신호 알림"
수준(지금처럼 텔레그램 알림 + UI 표시)이지 자동 주문 실행은 계속 범위
밖일 가능성이 높음. 다음 세션에서 구체적으로 논의 필요.

====================================================================
[NXT(넥스트트레이드) 시세 반영] (2026-08-04)
====================================================================
사용자 질문("ATRsite-pro NXT장은 반영이 안되는건가? 로직이 꼬이나?")으로
발견: kis_client.py의 FID_COND_MRKT_DIV_CODE가 "J"(KRX 전용)로 하드코딩돼
있어서 NXT 체결가가 애초에 조회 자체가 안 되고 있었음(로직 오류가 아니라
NXT 자체를 고려하지 않고 짜인 상태). 사용자가 명확한 라우팅 규칙을 지정함:
- ETF는 NXT에 상장되지 않으므로 시간과 무관하게 항상 KRX(J).
- ETF가 아닌 종목은 09:00~16:00(KST) 정규 시간대엔 무조건 KRX(J), 그 외
  시간(NXT 프리마켓/애프터마켓)엔 NXT(NX). "더 좋은 가격"(통합/UN) 기준이
  아니라 명시적 시간대 고정 라우팅이라는 점을 사용자가 특히 강조함.

구현: kis_client.py에 resolve_market_div_code(is_etf, now=None) 함수 추가,
get_current_price/get_daily_bars 양쪽에 연결. instruments 테이블에 is_etf
컬럼 추가(db.py에 기존 DB용 ALTER TABLE 보강 포함), 리포지토리/API
(InstrumentCreate/InstrumentSettingsUpdate)/프론트엔드(종목 추가 모달 +
종목 설정 패널에 "ETF" 체크박스)까지 전체 경로에 반영. 09:00/16:00은
market_schedule.py의 KRX 정규장 마감(15:30)과 일부러 다른 사용자 지정 값
-- 서로 다른 목적(폴링 주기 vs 시장 라우팅)이라 상수를 공유하지 않음.

참고로 사용자가 다음에 논의하자고 미뤄둔 것: "참조종목"(보유수량 0으로
설정해서 삼성전자/SK하이닉스 같은 시세만 참조하는 종목) 기능 -- v2의
크로스종목 조건부 매매 아이디어와 연결되는 개념. 이번 세션에서는 구현
안 함, 다음 세션에서 논의.

pytest 247개(신규 5개 포함) 전부 통과 확인. 아직 로컬 커밋/서버 배포는
안 함 -- 사용자 확인 대기 중.

====================================================================
[LLM 폴백 체인에서 제미나이 제외] (2026-08-04)
====================================================================
사용자가 실사용 기록(매크로 브리핑 7건 + 종목탐구 5건, 전부 예외 없이
"gemini")을 보고 "지금 LLM 리포트를 제미나이가 독점하다 하잖아?"라고 확인
요청 -- 실제로 그랬음(1순위라 한 번도 폴백이 발동한 적이 없었음). 사용자
판단: 제미나이를 신뢰하지 않음(환각 증상, 과거 데이터 버퍼 문제, 시황
웹서치 결과 괴리) -- "제미나이는 주석 처리하고 GPT로 옮겨봐."

llm_client.py의 _FALLBACK_CHAINS/_PROVIDER_ORDER에서 gemini를 뺐다(완전
삭제가 아니라 "주석 처리" 개념 -- GEMINI_API_KEY 설정과 _call_gemini()
구현 자체는 그대로 남겨둬서, 필요해지면 튜플에 "gemini"만 다시 넣으면
복구됨). 새 순서:
  - stage1_search(1단계, 객관조회): gpt -> claude
  - stage2_judgment(2단계, 판단): gpt -> deepseek -> claude

관련 주석(analysis_service.py/company_analysis_service.py의 "Gemini 우선"
설명)도 실제 순서에 맞게 갱신. 테스트 4개를 새 순서/제미나이 미사용에
맞게 다시 씀(제미나이 관련 기능 자체는 _call_gemini() 직접 호출로 별도
단위 테스트 유지). pytest 247개 통과 확인 후 실서버 배포 완료.

====================================================================
[stage2_judgment을 DeepSeek 우선으로] (2026-08-04)
====================================================================
사용자 지시: "stage2_judgment를 딥시크로 해봐." stage2_judgment(2단계,
판단) 체인을 gpt->deepseek->claude에서 deepseek->gpt->claude로 변경.
stage1_search(1단계, 웹서치 필요)는 DeepSeek가 검색 도구를 지원 안 해서
그대로 gpt->claude 유지(변경 없음). 클로드는 여전히 가장 비싸서 두 체인
모두 최후 폴백 자리.

테스트 2개를 새 순서에 맞게 다시 씀(DeepSeek 1순위 성공 케이스 +
DeepSeek->GPT->Claude 순차 폴백 케이스, 제미나이 미호출 재확인 포함).
pytest 247개 통과 확인 후 실서버 배포 완료.

====================================================================
[국내 종목 시세 조회 전면 실패 -- 워커 장기 실행 커넥션 문제] (2026-08-05)
====================================================================
사용자가 스크린샷으로 국내상장 종목(Kodex 200 등) 현재가가 전혀 안 뜨는
걸 신고, NXT 라우팅 버그로 의심함. 실제로 조사해보니:
- 최근 15분간 보유 종목 17개 전부(국내/해외, ETF/일반주식 가리지 않고)
  KIS에서 500 Internal Server Error, 성공 0건.
- 진단용으로 새 프로세스에서 단발성으로 조회하니 즉시 성공.
- atrsite-worker가 전날(08-04) 10:21부터 23시간 넘게 재시작 없이 떠있었음.

결론: NXT/ETF 라우팅 코드 버그가 아님(해외 종목까지 똑같이 실패한 게 결정적
증거 -- 그쪽은 이 로직을 아예 안 탐). 유력한 원인은 오래 떠있는 프로세스가
KIS와 맺은 HTTP 커넥션(연결 풀)이 상한 채로 남아 계속 재사용되다 실패를
반복한 것으로 추정 -- 새 프로세스/재시작 후의 클라이언트는 새 연결을 맺으니
바로 정상화됨. `sudo systemctl restart atrsite-worker atrsite-web`로 즉시
해결 확인(17개 종목 전부 재시작 직후 100% 성공).

재발 방지로 systemd/atrsite-daily-restart.{service,timer} 신규 추가 --
매일 새벽 08:00(장 시작 1시간 전, 저사용 시간대)에 atrsite-worker/
atrsite-web을 자동으로 깨끗하게 재시작한다. atrsite-backup.timer와 동일한
패턴(OnCalendar+Persistent=true). VPS에 배포하고 타이머 활성화 확인
(다음 실행: 2026-08-06 08:00 KST).

====================================================================
[v1.1 -- 현금 출금기록 기능] (2026-08-05)
====================================================================
사용자가 아주 상세한 스펙(0~17절)으로 요청. feature/cash-withdrawals
브랜치에서 작업, 1단계(기존 구조 조사) -> 2단계(DB+API+백엔드테스트)
-> 3단계(상단 nav 정리+출금기록 페이지) -> 4단계(CSV/최근용도/중복경고)
-> 5단계(회귀+병합+배포) 순서로 스펙이 요구한 순서 그대로 진행.

**조사 결과 스펙 초안과 실제 코드가 다른 부분(추측하지 않고 실제 구조에
맞춤)**:
- deposits 테이블엔 금융기관/계좌번호/활성화(is_active) 컬럼이 없음(계좌명+
  금액+통화뿐) -- "계좌 별칭 · 금융기관 · 통화" 표시는 "계좌명 · 통화"로,
  "비활성 계좌 제외" 요구사항은 그런 개념 자체가 없어 적용 안 함(TODO로 남김).
- URL 프리픽스는 스펙 초안의 /api/withdrawals가 아니라 기존 관례대로
  /api/v1/withdrawals, 수정 메서드는 PUT 아니라 기존처럼 PATCH.
- 금액은 스펙이 요청한 Decimal이 아니라 기존 전체 코드베이스와 동일하게
  REAL(float) -- SQLite 자체에 Decimal 타입이 없고 deposits.amount 등
  전부 REAL이라 이 기능만 다르게 하면 오히려 일관성이 깨짐(사용자에게
  이 편차를 보고할 것).

**DB**: cash_withdrawals 테이블 신규(withdrawn_at/deposit_account_id/
account_name_snapshot/purpose/amount/currency/memo/edited/created_at/
updated_at), 인덱스 4개. deposit_account_id는 FK ON DELETE SET NULL --
계좌가 삭제돼도 출금기록은 안 지워지고 account_name_snapshot으로 계속
식별됨(스펙 4 권장 정책). withdrawn_at은 created_at/updated_at(UTC ISO)과
다르게 Asia/Seoul naive 문자열로 저장(서버 시스템 타임존이 이미 KST라
market_schedule.py와 같은 전제). edited 컬럼은 "수정됨" 표시용(스펙 9.5) --
처음엔 updated_at != created_at으로 비교했다가 테스트로 실제 버그 발견
(초 단위 정밀도라 생성 직후 같은 초 안에 수정하면 구분이 안 됨), 명시적
플래그로 교체.

**API**(모두 API 키 인증 적용): GET/POST /api/v1/withdrawals, GET/PATCH/
DELETE /api/v1/withdrawals/{id}, GET .../summary(기간별), GET .../purposes
(최근 용도), GET .../export.csv(CSV, UTF-8 BOM+수식삽입 방지+내부ID 미포함),
POST .../check-duplicate(중복 경고용, 저장 자체는 안 막음). 정적 경로
(/summary, /purposes, /export.csv, /check-duplicate)는 전부 /{id}보다
먼저 등록(안 그러면 "summary" 같은 문자열이 {id}로 잡혀버림).

**기간 정의**(스펙 6): 오늘/이번주(월요일 시작)/이번달/YTD, 전부 "시작
이상, 다음 기간 시작 미만" 방식으로 경계 처리(23:59:59 문자열 비교 지양).
통화별로 따로 합산, 환율 환산 안 함(KRW/USD/JPY가 섞이지 않음). now를
주입 가능하게 만들어서(period_summary(now=...)) 월말/연말 경계 테스트를
결정론적으로 검증.

**프론트엔드**: 상단 헤더의 뒤로가기/앞으로가기/새로고침 3개 버튼과 관련
이벤트 리스너 제거(refreshCurrentView 함수 자체는 deposits.js/
instrument-detail.js/legacy-export.js가 계속 써서 남김). 새로고침 자리에
"출금기록"(💸) 아이콘 배치, report.html/company-explorer.html/briefing.html
과 같은 독립 페이지 패턴으로 withdrawals.html/css/js 신규 작성(target=_blank,
서비스워커 갱신 확인 블록 포함). .icon-btn 터치 영역을 40px->44px로 통일
(헤더의 모든 아이콘에 공통 적용). 출금액은 손익 마이너스(빨강)와 혼동 안
되게 앰버색으로 별도 표시. CSV는 <a href>가 아니라 인증 헤더 포함 fetch+
Blob 다운로드(API 키 배포 환경 대응).

**추천 기능**: 최근 용도 추천 칩(클릭해도 저장 안 되고 검색창만 채움),
중복입력 경고(계좌+금액+통화+일시+용도 전부 일치할 때만, 저장 자체는
안 막음), 계좌별 합계(접기/펼치기), "수정됨" 배지.

**테스트**: 백엔드 23개 신규(등록/조회/수정/삭제, 계좌 미등록/미존재 거부,
0/음수 금액 거부, 빈 용도 거부, 미지원 통화 거부, 용도 한글부분검색/영문
대소문자무시검색, 페이지 무관 전체 합계, 통화 미혼합, 계좌별합계, 예금
잔액/계좌 비연동 원칙 3종, 삭제된 계좌 스냅샷 유지, 인증 거부, CSV
한글/금액/내부ID미노출, 최근용도, 중복감지, 기간 경계값 4종). 프론트엔드는
실제 브라우저(격리된 임시 uvicorn 인스턴스 + SSH 터널, 운영 DB/서비스는
전혀 안 건드림)로 등록/수정/삭제/CSV/중복경고/데스크톱·모바일 레이아웃까지
실제 클릭 흐름으로 검증 -- 이 과정에서 실제 버그 2개 발견 후 수정:
(1) 위의 edited 컬럼 버그, (2) CSV 금액이 "60000.0"처럼 파이썬 float
그대로 찍히던 것(통화별 자릿수 맞춰서 수정).

전체 회귀: pytest 270개(기존 247 + 신규 23) 전부 통과, 기존 ATR/포지션/
거래/예금/KIS/LLM 기능 전부 그대로 동작 확인. feature/cash-withdrawals를
main에 병합 후 실서버 배포(사용자가 "묻지말고 자동으로 배포" 명시적 지시).

[2026-08-05 -- v1.2: 통합 투자 스케줄 및 예약알림 기능]

사용자가 22절짜리 상세 스펙으로 요청(BUY/SELL/DEPOSIT/WITHDRAWAL/REBALANCE/
RULE_REVIEW/MATURITY/DIVIDEND_CHECK/INTEREST_CHECK/EXCHANGE/DISCLOSURE_CHECK/
TAX_CHECK/GENERAL 12+1종 일정 유형, 반복 규칙, 시장별 휴장일 보정, 예산 배분,
텔레그램 예약알림). "묻지말고 자동으로 실제서버에 배포하고, 커밋해줘" +
"보고하지 말고 진행해. 최종 명령이야" 명시적 지시로 사전 보고 단계 없이
feature/investment-scheduler 브랜치에서 조사->구현->테스트->배포까지 전
과정을 자동 진행.

**스펙과 실제 구조의 괴리 확인**: 이 프로젝트엔 "계좌" 전용 테이블이 없다
-- deposits가 사실상 유일한 계좌 엔티티(현금/예금 계좌)라서, v1.1
cash_withdrawals와 동일하게 deposit_account_id(FK ON DELETE SET NULL) +
account_name_snapshot 패턴을 그대로 재사용했다. 기존 notification_outbox는
signal_event_id NOT NULL FK로 신호 알림 전용으로 이미 고정돼 있어(스키마를
깨지 않고는 재사용 불가), 상태 머신(PENDING/SENDING/SENT/RETRY/FAILED)과
재시도 백오프·telegram_client 발송 함수만 그대로 재사용하고 별도
schedule_notification_outbox 테이블을 새로 만들었다(idempotency는
UNIQUE(occurrence_id, notification_type, scheduled_date)).

**DB(신규 8테이블)**: investment_plans/investment_plan_items(계획+종목배분),
investment_schedules/investment_schedule_items(반복 규칙+종목배분),
schedule_occurrences/schedule_occurrence_items(실제 생성된 회차+회차별
종목금액), schedule_executions(계획 대비 실제 실행 기록 -- trades/
cash_withdrawals/deposits를 절대 자동 생성/수정하지 않고 링크만 하거나
링크 없이 "완료 처리"만 남김), schedule_notification_outbox(예약알림
발송 큐). investment_schedules.notify_days_before(기본 0)로 "며칠 전
알림" 지원.

**schedule_engine.py(순수함수, services/)**: generate_occurrence_dates
(ONCE/DAILY/WEEKLY/BIWEEKLY/MONTHLY/YEARLY 날짜 확장, MONTHLY/YEARLY는
말일 클리핑 처리, CUSTOM은 명시적 날짜 목록, occurrence_count/end_date
둘 다 없으면 명시적으로 거부해 무한생성 방지 + MAX_OCCURRENCES=2000 안전
장치), adjust_for_holiday(KRX는 기존 market_schedule.is_trading_day를
그대로 재사용해 실거래 로직과 동일 근거 유지, NYSE_NASDAQ/JPX는 검증된
휴장일 데이터가 이 프로젝트에 전혀 없어 주말만 보정하고 회차마다
needs_confirmation=True를 무조건 세움 -- "확인 못 하면 날짜를 추측해서
확정하지 않는다"는 스펙 원칙을 평일에도 지키기 위함), allocate_evenly/
allocate_by_ratio(나머지는 항상 마지막 항목에 배분).

**실제 ISA 사례로 검증**: 74,918,796원을 9회 주간 분할, 회차마다 TIGER
KOFR금리액티브(449170)/KODEX iShares 미국하이일드액티브(468380) 50:50
배분 -- 8회는 8,324,310원(회차별 4,162,155원씩), 마지막 1회만 나머지
포함 8,324,316원(4,162,158원씩), 합계 74,918,796원 정확히 일치를 단위
테스트+API 통합테스트+실제 브라우저 등록 흐름까지 3중으로 검증.

**API**(/api/v1/investment-plans, /api/v1/investment-schedules,
/api/v1/schedule-occurrences, 전부 API 키 인증): 계획/스케줄 CRUD(PATCH는
반복규칙·시작일은 수정 대상에서 제외 -- 이미 생성된 회차와 얽혀 있어
바꾸려면 취소 후 재등록), 스케줄을 CANCELLED로 바꾸면 아직 안 끝난
(SCHEDULED/NOTIFIED) 회차도 함께 취소, GET .../today(오늘분+지연분),
GET .../badge-count(SCHEDULED-도래분/NOTIFIED-미확인/PARTIALLY_EXECUTED/
OVERDUE만 집계, COMPLETED/SKIPPED/CANCELLED/ARCHIVED/미래일정 제외,
9+ 표시 캡), POST .../{id}/executions(실행 기록, 기존 cash_withdrawals/
trades id를 검증 후 링크 가능).

**텔레그램 예약알림**: schedule_notification_service.py가 매일 1회(시장
상태와 무관 -- GENERAL/RULE_REVIEW 등은 휴장일에도 알림 필요) 오늘 알릴
회차를 outbox에 적재(enqueue_due_notifications, UNIQUE 제약으로 중복
적재 자체가 불가능해 진짜 멱등), 매 폴링 주기 발송(재시도 백오프는
notification_service.py와 동일 정책 재사용: 30초/2분/10분). 발송 성공하면
회차 상태를 자동으로 NOTIFIED로 전환. worker.py의 run_once()에 시장
phase와 무관한 별도 흐름으로 통합.

**프론트엔드**: withdrawals.html과 동일한 독립 페이지 패턴으로
investment-schedule.html/css/js 신규 작성(오늘/지연 아젠다 위젯, 유형·
상태·기간 필터, 카드형 목록, 일정 등록 모달 -- 반복유형에 따라 간격/
종료방식 필드 동적 표시, 종목 배분 행 추가/삭제, 회차 상세 모달에서
확인/완료/건너뜀/취소 처리). 상단 헤더에 📅 아이콘 + 배지(schedule-badge)
추가, badge-count API로 채움. service-worker.js v15->v16.

**테스트**: 백엔드 48개 신규(schedule_engine 순수함수 22개 -- 반복규칙
확장/월말·윤년 클리핑/휴장일보정/예산배분/ISA 실사례, API 통합 17개 --
CRUD/휴장일확인플래그/배지/오늘위젯/실행연결/ISA 전체 시나리오, 알림
서비스 9개 -- 멱등성/재시도/실패전환/메시지포맷). 전체 회귀 318개
(기존 270 + 신규 48) 전부 통과. 프론트엔드는 격리된 임시 uvicorn +
SSH 터널(운영 DB/서비스 미접촉)로 ISA 9회 시나리오를 실제 브라우저에서
등록->목록 확인(정확히 8,324,310원x8 + 8,324,316원 표시)->상세 모달
(종목별 4,162,155원 표시)->완료 처리->지연 배지(1) 표시까지 전 과정
실제 클릭/폼입력으로 검증, 콘솔 에러 없음 확인.

feature/investment-scheduler를 main에 병합 후 실서버 배포(사용자가
"묻지말고 자동으로 배포, 커밋" + "보고하지 말고 진행" 명시적 재확인).

[2026-08-05 -- 실사용 ISA 플랜 등록 + 일정 수정/삭제 기능 추가]

배포 직후 사용자가 실제 ISA 매수 계획표(2026-08-05 시작, 매주 수요일 9회,
회차당 900만원 고정·9회차만 잔여분 2,918,796원)를 그대로 등록해달라고
요청. 이 배분은 schedule_engine.allocate_evenly(총액을 회차수로 균등분할)
로는 낼 수 없는 모양이라(74,918,796/9=회차당 8,324,310원, 사용자가 원한
"회차당 900만원 고정"과 다름), 반복 스케줄 1개 대신 같은 investment_plans
에 묶인 ONCE 스케줄 9개로 등록해 회차별 실제 금액을 정확히 맞춤(TIGER
KOFR금리액티브 종목 신규 등록 포함). 프로덕션 DB에 직접 리포지토리 함수를
호출해 반영(운영 API 키를 화면에 노출하지 않기 위해 서버 안에서 조립),
합계 74,918,796원 정확히 일치 확인.

이어서 사용자가 "등록된 스케줄에 수정과 삭제가 없다"고 지적 -- 실제로
DELETE는 API에 이미 있었지만 UI에 버튼이 없었고, PATCH는 title/
holiday_policy/status/memo만 받아서 날짜·금액·종목배분은 전혀 못 고치는
상태였음(막 등록한 ISA 회차의 오타/금액을 고칠 방법이 없었음). 다음과
같이 확장:

- update_schedule() 확장: title/market/holiday_policy/notify_days_before/
  status/memo/deposit_account_id는 반복 유형과 무관하게 언제든 수정 가능.
  start_date/total_amount/currency/items(날짜·금액·종목배분)는 ONCE
  일정에서만 허용 -- 반복 일정은 회차가 여러 개라 "어느 회차를 바꾸는지"
  모호해지므로 거부(취소 후 재등록 안내). 그 유일한 회차가 이미 완료/
  건너뜀/취소/부분실행이면 그것도 거부(처리된 계획을 조용히 바꾸지 않음).
  market/holiday_policy가 바뀌면 아직 확정 전(SCHEDULED/NOTIFIED)인 회차만
  새 정책으로 휴장일 재계산(이미 지난 회차는 안 건드림) -- 기존에는 이
  두 필드를 PATCH해도 이미 생성된 회차에는 아무 효과가 없던 잠재 버그였음,
  이번에 같이 고침.
- 프론트엔드: 회차 상세 모달에 "일정 수정"/"일정 삭제" 버튼 추가. 수정은
  기존 등록 모달을 재사용하되(제목이 "일정 수정"/버튼 "저장"으로 바뀜),
  반복 일정이면 구조적 필드(반복유형/간격/시작일/종료방식/총금액/통화/
  계좌/종목배분)를 disabled로 잠가서 백엔드가 거부할 조합을 애초에 못
  만들게 함. 상태(진행중/일시중지/완료/취소) select는 수정 모드에서만
  표시. 삭제는 확인창 후 DELETE 호출, 반복 일정이면 회차 개수를 미리
  경고에 표시.
- 실전에서 잡은 버그 1개: "새 일정" 버튼 리스너를
  `addEventListener('click', openScheduleModal)`로 직접 연결해뒀는데,
  openScheduleModal이 이번에 `scheduleToEdit` 매개변수를 받게 되면서
  클릭 이벤트 객체가 그대로 그 자리로 들어가 신규 등록이 깨질 뻔함 --
  배포 전 코드 검토로 발견, 인자 없는 화살표 함수로 감싸서 수정.

테스트: 백엔드 7개 신규(ONCE 금액/종목배분 수정 후 재계산, 시작일 수정 시
휴장일 재계산, 반복 일정 금액수정 거부, 완료된 회차 수정 거부, 제목/메모는
반복 여부 무관하게 항상 허용, 휴장정책 수정 시 미확정 회차만 재계산, 삭제
캐스케이드). 전체 회귀 325개 통과. 프론트엔드는 격리된 임시 uvicorn+SSH
터널로 ONCE/반복 두 케이스 모두 실제 브라우저에서 수정(금액 변경->회차별
1,459,398원 재계산 확인)/반복 일정 필드잠금/삭제(카드가 목록에서 사라짐)
까지 실제 클릭으로 검증, 콘솔 에러 없음 확인.

main에 병합 후 실서버 배포(사용자가 이전에 준 "묻지말고 자동으로 배포,
커밋" 지시가 이 세션 내 계속되는 동일 작업 흐름이라 그대로 적용).

[2026-08-05 -- v1.3: 일별 총자산·손익 스냅샷 및 자산추이 차트]

사용자가 22절짜리 상세 스펙으로 요청(매일 06:30 KST 스냅샷 저장, 총자산/
손익/통화별 자산/데이터품질 기록, 기간별 차트). 스펙 자체가 "구현 전 타당성
검토 보고 후 승인 받고 진행"을 요구해서, 먼저 20개 항목 조사 후 구현가능
범위/재사용엔진/외화환산 방식/차트 라이브러리 3가지를 질문으로 보고. 사용자가
"구현 전 검토·질문은 너가 추천하는 대로 임의로 진행하라" + "운영 서버 배포는
로컬 구현·테스트 완료 후 자동으로 배포후 커밋 완료한다"고 확정 지시.

**조사 결과 요약**: 총자산/평가손익 공식은 기존 `portfolio_service.
build_dashboard()`에 이미 정확히 있었음(종목평가액+예금합계, 평가액－매입
원가) -- 그대로 재사용. 실현손익은 저장된 적 없지만 `position_engine.
replay_trades()`가 매도마다 계산하는 값을 종목별 전체 거래 재생으로 누적하면
집계 가능(신규 데이터 불필요). 환율은 수동 입력만 있고 자동 갱신이 없어서(운영
DB 확인: USD 1449.28, 3일 전 값) 권장안(A) 수동값+오래됨 경고를 채택. 차트
라이브러리는 없어서 Chart.js 4.4.4(MIT, UMD 단일파일 ~200KB)를 CDN 없이
`frontend/vendor/chart.umd.min.js`로 다운로드해 저장소에 직접 커밋(오프라인/
네트워크 장애에도 차트가 안 깨지게).

**DB(신규 2테이블)**: portfolio_daily_snapshots(총합, snapshot_date UNIQUE),
portfolio_snapshot_items(종목별/예금별 상세 -- item_type INSTRUMENT/DEPOSIT).
금액은 이 프로젝트 관례대로 REAL, 계산 불가능한 값은 가짜 0이 아니라 NULL로
남김(스펙 8절 "부분 실패 시 이전 값을 복사하지 않는다" 취지).

**snapshot_service.py**: build_dashboard()와 동일한 통화환산(_convert 재사용)
+ 누락처리 원칙을 그대로 따라 대시보드와 스냅샷 총자산이 항상 일치하도록 함.
market_schedule.compute_data_status()로 시세 신선도를 재계산해 STALE/API_ERROR면
STALE_PRICE로 표시(단, 수동입력 시세는 이 재계산에서 제외 -- 기존 함수 자체
문서화 원칙). data_quality_status는 FAILED>PARTIAL>STALE_PRICE>COMPLETE
순으로 판정, 관리자 수동 실행이면 COMPLETE를 MANUAL로 재라벨링.
한계(사용자에게 보고): 외화 거래 실현손익은 거래시점 환율이 저장돼 있지 않아
"현재" 환율로 근사 환산함 -- 시점별 정확한 원화 환산은 아님(코드 주석에 명시).

**Worker(06:30/06:40/07:00 최대 3회 재시도)**: 새 스케줄러 라이브러리 없이
기존 worker.py 폴링 루프에 날짜-가드 패턴(v1.2 예약알림과 동일 방식)으로 통합.
휴장일엔 원래 30분 폴링 간격이라 재시도가 그 시각 근처에 안 일어날 수 있어서,
06:15~07:15 구간만 시장 phase와 무관하게 60초 간격으로 강제. 이미 COMPLETE류
스냅샷이 있으면 재시도 자체를 건너뜀(멱등). 실패/부분/2일이상누락일 때만
텔레그램 경고(성공 시엔 알림 안 보냄 -- 스펙 18절), 발송 실패해도 워커 루프는
절대 안 멈춤.

**API**(/api/v1/portfolio-snapshots, API 키 인증): GET .../latest, GET .../chart
(period=1m/3m/6m/1y/all 또는 start_date+end_date, 응답에 스펙 최소목록+환율/
품질메모까지 포함), GET .../{date}, GET .../{date}/items, POST .../run(관리자
수동 재계산, 이미 완료여도 강제 갱신), POST .../{date}/recalculate(오늘 날짜만
허용 -- 과거 시세·환율을 정확히 복원할 수 없어서 스펙 17절에 따라 거부).

**프론트엔드**: withdrawals.html과 동일한 독립 페이지 패턴으로 asset-history.
html/css/js 신규 작성. 최신현황 카드(총자산/손익/수익률/투자자산/현금예금/
데이터품질뱃지), 기간버튼(1개월~처음부터, 기본 3개월)+사용자지정기간, 차트
전환(총자산/손익/수익률 -- 같은 Y축에 안 겹침), 백만원 단위 표시(Math.trunc,
DB는 원 단위 그대로)+터치 시 정확한 원 단위 툴팁(환율/데이터품질 포함), 가로모드
CSS(orientation:landscape)+확대보기(진짜 Fullscreen API 대신 오버레이 모달로
대체 -- 스펙이 명시적으로 허용한 폴백, 모바일 Edge 호환성 문제를 원천 회피).
헤더에 투자일정 옆 📈 아이콘 + 배지(오늘 스냅샷 없거나 PARTIAL/FAILED일 때만
경고 표시, 정상이면 숨김). service-worker v16->v17.

**실전에서 검토 중 확인한 사항**: 별도 실패 방지 인프라(재시도용 Outbox)는
신규로 안 만들고 telegram_client.send_message()를 워커에서 직접 호출 -- 이
알림은 실패해도 스냅샷 자체는 이미 저장돼 있어 재전송 필요성이 낮다고 판단.

**테스트**: 백엔드 31개 신규(snapshot_service 12개 -- 대시보드 일치/통화별
분해/실현손익 집계/전량매도 후에도 실현손익 유지/시세·환율 누락/FAILED/
STALE_PRICE/수동입력 재판정 제외/MANUAL 재라벨링, API 13개 -- CRUD/멱등성/
과거날짜 재계산 거부/기간 필터/인증, worker 6개 -- 06:30 이전 미실행/정시
실행/중복방지/재시도 후 성공시 upsert/3회 소진 후 포기/휴장일 폴링간격
단축). 전체 회귀 356개 통과. 프론트엔드는 격리된 임시 uvicorn+SSH 터널로
60일치 시드 데이터를 만들어 최신현황 카드/기간버튼(1개월~처음부터)/사용자
지정기간/차트전환(총자산·손익·수익률)/확대보기 오버레이/다시계산 버튼까지
실제 클릭으로 검증, 가로모드 리사이즈 포함 콘솔 에러 없음 확인. 헤더 배지도
"오늘 스냅샷 없음" 상태에서 정상적으로 경고(!) 표시되는 것 확인.

feature/portfolio-snapshots를 main에 병합 후 실서버 배포(사용자가 "로컬
구현과 테스트가 모두 완료된 후 자동으로 배포후 커밋 완료한다. 최종 결정이다"
명시적 지시).

[2026-08-06 -- "주가 갱신" 버튼에 환율 자동조회 연결]

v1.3 자산 스냅샷 작업 때부터 알려진 한계(환율이 수동 입력만 있고 자동 갱신이
없음, GPT가 지적한 "외화 실현손익 근사치" 문제와도 연결됨)에 대해 사용자가
"환율을 주가갱신 버튼을 누르면 환율도 같이 자동으로 갱신되도록 해"라고 지시.

adapters/fx_rate_client.py 신규: frankfurter.dev(ECB 기준환율, API 키 불필요 --
이 프로젝트 안에서 2026-08-02에 USD/KRW 수동 채우기로 이미 한 번 검증된 소스)
로 조회. market_index_client.py와 동일 원칙 -- 실패해도 예외를 안 던지고 None만
반환, 실패한 통화는 기존 저장값을 그대로 둔다(조용히 지우거나 0으로 덮지 않음).

portfolio_service.refresh_all_quotes_now()를 확장: 종목 시세 갱신에 이어 실제로
쓰이고 있는 통화(활성 종목 + 예금 계좌, KRW 제외)만 환율도 같이 조회해서
fx_rates에 반영(source="auto"로 구분, 수동 입력과 값은 섞이지만 출처는 남음).
worker.py의 5분 자동 폴링에는 연결하지 않음 -- ECB 환율은 하루 한 번만 갱신돼서
5분마다 다시 조회할 이유가 없고, 사용자가 요청한 범위(수동 "주가 갱신" 버튼)만
반영.

프론트엔드(dashboard.js): 갱신 결과 요약에 "환율 갱신: USD, JPY" / "환율 갱신
실패: ..."를 종목 결과와 함께 한 줄로 표시. service-worker 캐시 v17->v18.

테스트: fx_rate_client 6개(USD/JPY 파싱, KRW 단축경로 네트워크 호출 안 함,
필드누락/네트워크오류/HTTP오류 시 None), portfolio_service 3개(사용 중인 통화만
조회/KRW전용 포트폴리오는 조회 자체를 안 함/실패 시 기존 값 유지 확인) 신규.
전체 회귀 365개 통과. 격리된 미리보기 서버에서 실제 frankfurter.dev를 호출해
USD 1423.72/JPY 9.0343을 정확히 받아와 source="auto"로 저장되는 것까지 실제
네트워크로 검증(목업 아님).

main에 직접 커밋 후 실서버 배포(이 세션 내 계속되는 "로컬 완료 후 자동 배포"
지시를 그대로 적용).

--------------------------------------------------------------------
[GPT 코드 점검 보고서 대조 검증 + 채택 4건 수정 (신호소실/XSS/restore_check/lock)]
(2026-08-06)
--------------------------------------------------------------------

사용자가 GPT가 작성한 코드 점검 보고서(커밋 7838222 기준, [중요1][중요2]
[중간1-5][낮은위험] 항목 다수)를 붙여넣고 "불가능한건 불가능하다고, 불필요한건
불필요하다고, 채택할만건 채택해"라고 지시. GPT는 실제 코드를 볼 수 없으므로
항목 전부를 실제 소스와 직접 대조해서 검증한 뒤 진짜인 것만 채택:

- 허위경보/이미 해결됨으로 판단한 것: (여기 항목은 채택 목록 밖이라 생략,
  보고서 대조 과정에서 코드로 직접 확인)
- 채택 4건(사용자가 "1~4번을 제안한 순서대로 진행"이라고 승인):

1) [중요1] 신호 소실 버그 -- data_status가 STALE/API_ERROR/INSUFFICIENT_DATA로
   막히면 signal_engine.determine_signal()이 항상 NEUTRAL/NO_POSITION을
   반환하도록 설계돼 있는데(오래된 데이터로 새 신호를 내지 않으려는 의도적
   설계), portfolio_service.recompute_signal()이 이 값을 무조건
   signal_state.status에 덮어써서 -- 예를 들어 이미 떠 있던 STOP_TRIGGERED
   경고가 시세 조회 한 번 실패하는 순간 조용히 사라져 버리는 실제 결함이었음
   (코드 직접 추적으로 확인, 허위경보 아님).
   수정: recompute_signal()에 조기 분기 추가 -- previous가 존재하고
   data_status가 막힌 상태면 signal_events/notification_outbox는 전혀
   건드리지 않고(새 이벤트/중복 텔레그램 알림 없음), repositories/signals.py에
   새로 만든 update_data_status_only()로 data_status/next_buy_price/
   take_profit_price/trailing_stop_price/computed_at만 갱신. status/reason/
   latest_event_id/acknowledged_event_id(확인 상태)는 완전히 그대로 유지.
   신규 종목의 최초 계산(previous가 없는 경우)은 이 분기를 타지 않고 기존
   경로 그대로(어차피 안전한 초기값이 나옴).
   회귀 테스트 5개 추가: STALE 발생 시 STOP_TRIGGERED 유지+확인상태 유지,
   API_ERROR도 동일하게 보존, 데이터 정상 복귀 후 조건 불변이면 계속 유지,
   조건이 실제로 바뀌면(가격이 관망 구간으로) 정상적으로 해제, 신규 종목
   최초 계산은 안전하게 초기화.

2) [중간2] XSS -- withdrawals.js/legacy-export.js에서 서버 응답 필드를
   innerHTML에 그대로 삽입하는 곳 발견(둘 다 실제 결함). 지시대로 "확인된
   두 곳과 같은 파일의 사용자 입력 innerHTML"을 전수점검한 결과, GPT가
   못 짚은 세 번째 지점을 발견 -- withdrawals.js는 account_name_snapshot을
   카드 메타/삭제확인 모달 두 곳에서는 이미 자체 escapeText()로 이스케이프
   하고 있었는데, 계좌별 요약(account-breakdown) 렌더링 한 곳만 빠져있던
   불일치 버그였음(헬퍼 자체가 없던 게 아니라 적용이 일관되지 않았던 것).
   legacy-export.js는 이런 헬퍼가 아예 없어서 escapeHtml() 신규 작성 후
   에러/경고 목록의 path/message 필드에 적용.
   VPS 격리 미리보기로 실제 <img src=x onerror=alert(1)> 페이로드를 예금
   계좌명으로 넣어 브라우저에서 직접 검증 -- 이스케이프된 텍스트로만
   렌더링되고 alert 실행 안 됨 확인.

3) [중간4] scripts/restore_check.py의 CORE_TABLES -- v1.0 초기 8개 테이블
   에서 갱신된 적이 없어서 v1.0 후반(company_explorer)과 v1.1~v1.3에서
   추가된 테이블(cash_withdrawals/investment_schedules/schedule_occurrences/
   portfolio_daily_snapshots/portfolio_snapshot_items/analysis_results/
   companies/company_* 등)이 전부 빠져 있었음 -- 실제 재해복구 상황에서
   대부분의 최신 데이터가 복구됐는지 검증조차 안 되는 상태였음. 지시대로
   analysis_results/company_*/v1.1~v1.3 전체 테이블까지 포함해서 8개 ->
   29개로 확장(버전별 주석 포함, daily_bars/atr_values/quote_latest 등은
   의도적으로 제외한 이유도 주석에 명시). 새 테스트 파일
   tests/test_restore_check.py 3개 추가(실제 스키마와 대조, 버전별 핵심
   테이블 누락 가드, 중복 없음).

4) [낮은위험] requirements.txt/requirements-dev.txt는 직접 의존성만 고정
   (각 5개/6개)하고 starlette/httpcore/anyio 같은 간접 의존성은 설치
   시점마다 달라질 수 있었음. RackNerd VPS에서 완전히 새로 만든 클린
   Python 3.12.3 venv 두 개(운영용은 requirements.txt만, 개발용은
   requirements-dev.txt까지)에 각각 설치해서 나온 전체 트리를 pip freeze로
   그대로 고정 -- requirements-lock.txt(22개)/requirements-dev-lock.txt
   (26개, pytest 등 개발용만 추가로 포함) 신규 생성. 운영 lock에 pytest 같은
   개발 전용 패키지가 섞이지 않도록 두 venv를 처음부터 분리해서 만듦.
   DEPLOY.md 3절에 재해복구/신규설치 시 lock 파일 사용법 추가(기존 8절
   "일상 재배포" 절차는 그대로 유지, loose requirements.txt 계속 사용).

테스트 작성 중 발견한 내 실수 3건(전부 테스트 코드 문제, 실제 결함 아님) --
record_trade()가 매수 시점에 이미 신호를 1회 계산해서 이벤트 개수 기준값이
1이 아니라 2였던 것/회복 시나리오의 "회복 가격"이 우연히 다른 임계값(익절선)
을 건드려서 기대값이 틀렸던 것/헬퍼 반환값 튜플 크기 변경 후 호출부 갱신
누락 -- 전부 수정 후 재실행.

frontend/js/legacy-export.js, frontend/js/withdrawals.js 변경으로
service-worker.js CACHE_NAME v18->v19.

전체 회귀 373개 통과(기존 365개 + 이번 신규 8개: 신호소실 5개 + restore_check
3개). main에 직접 커밋 후 실서버 배포.
