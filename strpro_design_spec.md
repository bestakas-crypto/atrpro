# ATRsite-pro 전면 설계 및 기존 코드 검토 보고서

**문서 버전:** 1.0
**검토 기준일:** 2026년 8월 1일
**검토 대상:** ATRsite 기존 PWA 코드와 ATRsite-pro 서버형 전환안

---

## 1. 최종 결론

ATRsite-pro는 다음 구조로 개발하는 것이 가장 적절하다.

### 최종 권장 구조

* 기존 HTML·CSS 기반 모바일 UI는 최대한 유지
* 계산과 신호 판정의 최종 권한은 서버로 이동
* 하나의 저장소 안에서 웹 서버와 시세 Worker를 별도 프로세스로 실행
* 두 프로세스는 동일한 SQLite DB와 공통 계산 모듈 사용
* 현재가와 확정 일봉은 한국투자증권 API에서 수집
* ATR은 확정 일봉으로 하루 한 번 갱신
* 장중에는 확정된 ATR로만 기준선을 계산
* 신호 상태가 실제로 바뀔 때만 텔레그램 발송
* 사용자가 MTS·HTS에서 체결한 뒤 앱에 실제 체결을 등록
* 자동 주문 기능은 구현하지 않음
* DB 백업은 서버 2개, 외부 저장소 14개로 고정 순환
* PWA 오프라인 기능은 마지막 상태 열람 전용으로 제한

### 채택하지 않을 구조

* 자동 주문·자동 체결
* 다중 사용자 회원가입
* Redis
* Celery
* 메시지 브로커
* PostgreSQL
* Docker 다중 컨테이너
* 장중 ATR 실시간 변동
* 텔레그램 채널을 주 백업 저장소로 사용하는 방식
* 웹 서버와 Worker 사이의 외부 웹훅
* 모든 시세를 무기한 저장하는 구조

현재 Oracle 공식 문서상 Always Free Ampere A1 한도는 합계 2 OCPU·12GB로 안내되고 있다. 과거에 널리 알려진 4 OCPU·24GB와 다르므로 실제 계정 콘솔의 현재 할당량을 확인해야 한다. 본 프로그램은 AMD 1GB에서도 축소 운용할 수 있도록 설계하되, 기본 배포는 ARM 환경을 기준으로 한다.

---

# 2. 절대 원칙

## 2.1 프로그램의 역할

ATRsite-pro는 주문 프로그램이 아니라 다음을 수행하는 **의사결정 보조 시스템**이다.

* 현재 시세 수집
* 확정 일봉 수집
* ATR 계산
* 다음 매수가 계산
* 익절 기준가 계산
* 추적 손절선 계산
* 기준 도달 여부 판정
* 사용자에게 알림
* 수동 체결 기록
* 자산 및 거래 이력 관리

실제 주문은 항상 사용자가 증권사 HTS·MTS에서 직접 수행한다.

## 2.2 제외 범위

다음 기능은 향후에도 구현하지 않는다.

* 주문 API 호출
* 자동 매수·자동 매도
* 예약 주문
* 계좌 자금을 자동으로 이동하는 기능
* 앱이 독자적으로 투자금액이나 비중을 결정하는 기능
* 다중 사용자 권한 관리
* 소셜·커뮤니티 기능

---

# 3. 기존 ATRsite 코드 평가

## 3.1 현재 코드의 장점

기존 ATRsite는 단순한 프로토타입 수준을 넘어 실제 사용에 필요한 주요 기능이 이미 구현되어 있다.

* 자유로운 종목 등록
* 매수·매도 거래 이력
* 평균단가와 보유수량
* 다음 매수가
* 익절가
* 레칫 방식 추적 손절선
* 5단계 신호 표시
* 부분매도 시뮬레이션
* 최고가 대비 하락 구간
* 예금 관리
* 다중 통화 환산
* 기존 버전 데이터 마이그레이션
* PWA 설치와 오프라인 캐시

현재 구조는 순수 HTML·CSS·JavaScript이며, `stock-portfolio`, `cash-deposits`, `fx-rates`를 localStorage에 저장한다. UI는 종목 목록·종목 상세·거래 입력 모달로 구성되어 있고, 모바일 화면에 맞춰 설계되어 있다.
특히 추적 손절선은 기존 값과 새 후보값 중 큰 값을 저장하는 방식으로 구현되어 있어 레칫 원칙 자체는 정확하다.

서비스워커도 기존 캐시 고착 문제를 해결하기 위해 네트워크 우선 전략으로 변경되어 있다.

---

# 4. 기존 코드에서 반드시 수정해야 할 문제

## 4.1 매도 후 재매수 시 평균단가 오류

### 심각도: 매우 높음

현재 `computeDerived()`는 매도 시 실현손익만 계산하고 `runQty`와 `runCost`를 줄이지 않는다. 따라서 매도 이후 다시 매수하면 잔여 보유분이 아니라 과거의 모든 매수 수량을 기준으로 평균단가를 계산하게 된다.

예를 들어 다음 거래가 있다고 가정한다.

```text
10주 × 100원 매수
5주 × 120원 매도
5주 × 200원 재매수
```

정상적인 이동평균 원가 방식:

```text
잔여 5주 원가: 500원
신규 5주 원가: 1,000원
최종 보유수량: 10주
최종 평균단가: 150원
```

현재 코드 계산:

```text
전체 매수수량: 15주
전체 매수원가: 2,000원
표시 평균단가: 133.33원
```

이 오류는 다음 항목을 모두 왜곡한다.

* 평균단가
* 매입금액
* 평가손익
* 익절가
* 부분매도 예상손익
* 이후 실현손익
* 종합자산 손익

### 수정 원칙

매도 시 다음과 같이 처리해야 한다.

```text
매도 직전 평균단가 = 보유원가 ÷ 보유수량
실현손익 = (매도가 - 평균단가) × 매도수량
잔여원가 = 기존 보유원가 - 평균단가 × 매도수량
잔여수량 = 기존 보유수량 - 매도수량
```

전량 매도 후에는 보유원가와 평균단가를 0으로 초기화한다.

---

## 4.2 보유수량을 초과한 매도 입력 가능

### 심각도: 매우 높음

현재 거래 저장 로직은 단가와 수량이 양수인지 확인하지만, 매도수량이 보유수량을 초과하는지는 확인하지 않는다.

따라서 10주를 보유한 상태에서 15주 매도를 등록할 수 있고, 결과는 다음처럼 될 수 있다.

```text
보유수량: -5주
매입금액: 음수
평가금액: 음수
부분매도 수량: 음수
```

### 수정 원칙

* 신규 매도: 매도수량은 현재 보유수량 이하
* 과거 거래 수정: 해당 거래 시점의 보유수량을 다시 계산한 뒤 검증
* 거래 삭제·수정 후 전체 거래 이력을 시간순으로 재검증
* 어느 시점에서든 보유수량이 음수가 되면 저장 거부

서버에서는 HTTP 409 또는 검증 오류로 처리하고, 화면에는 해당 시점의 최대 매도 가능 수량을 표시한다.

---

## 4.3 미보유 상태에서도 익절 신호가 발생할 수 있음

### 심각도: 높음

현재 익절가는 다음과 같이 계산된다.

```text
평균단가 + ATR × 매도배수
```

보유 거래가 없으면 평균단가가 0이므로 현재가와 ATR을 입력했을 때 보유하지 않은 종목에서도 익절 신호가 발생할 수 있다.

### 수정 원칙

신호 계산에는 포지션 상태를 선행 조건으로 사용한다.

| 상태       |    매수 신호 |    익절 신호 |    손절 신호 |
| -------- | -------: | -------: | -------: |
| 미보유      |       가능 |       불가 |       불가 |
| 보유       |       가능 |       가능 |       가능 |
| 전량 매도 완료 |       가능 |       불가 |       불가 |
| 데이터 지연   | 신규 신호 금지 | 신규 신호 금지 | 신규 신호 금지 |

---

## 4.4 다음 매수가의 기준가격 문제

### 심각도: 높음

현재 `getBasePrice()`는 가장 최근 거래가 매수인지 매도인지 구분하지 않고 마지막 거래가격을 반환한다. 따라서 부분매도를 하면 다음 매수가가 마지막 매도가를 기준으로 다시 계산된다.

예:

```text
100원 매수
120원 부분매도
ATR 10원
```

현재 코드:

```text
다음 매수가 = 120 - 10 = 110원
```

분할매수 그리드 관점에서 더 일관된 계산:

```text
다음 매수가 = 마지막 매수가 100 - 10 = 90원
```

### 최종 결정

ATRsite-pro의 기본값은 **마지막 매수가 기준**으로 변경한다.

```text
다음 매수가 = 마지막 매수가 - 현재 확정 ATR × 매수배수
```

따라서 DB에 다음 값을 별도로 유지한다.

* `last_buy_price`
* `last_sell_price`
* `last_trade_price`

기존 동작을 보존할 필요가 있을 때만 `LAST_TRADE` 모드를 선택사항으로 둘 수 있지만, 초기 버전에서는 추가하지 않는 편이 낫다.

---

## 4.5 거래 순서가 날짜만으로 관리됨

현재 거래에는 날짜만 있고 시각이나 별도 순서값이 없다.

같은 날 다음 거래가 발생하면 서버가 정확한 순서를 알 수 없다.

```text
오전 매수
오후 부분매도
마감 전 재매수
```

### 수정 원칙

각 거래에는 다음을 저장한다.

* `executed_at`: 실제 체결일시
* `sequence_no`: 같은 시각 또는 날짜 충돌 시 정렬값
* `created_at`
* `updated_at`

모바일 입력에서는 기본값을 현재 시각으로 설정하되 수정할 수 있게 한다.

---

## 4.6 최고가 계산 방식 보완

현재 앱은 사용자가 입력한 현재가가 기존 최고가보다 높을 때 최고가를 갱신한다.

5분 폴링에서는 다음 상황이 생길 수 있다.

```text
10:02 장중 최고가 105,000원
10:05 조회 현재가 103,000원
```

현재가만 사용하면 105,000원을 놓치게 되고 추적 손절선이 실제보다 낮아질 수 있다.

### 수정 원칙

최고가는 우선순위에 따라 갱신한다.

1. API가 제공하는 당일 고가
2. 확정 일봉의 고가
3. 폴링 중 관측한 현재가
4. 사용자 수동 입력값

```text
새 보유 후 최고가 =
MAX(기존 보유 후 최고가, 당일 고가, 현재가)
```

---

## 4.7 거래 수정 시 추적 손절 상태 재검증 부족

거래 수정이나 삭제 후 현재 코드는 보유수량이 0인지, 최고가가 비어 있는지만 점검한다. 과거 첫 매수일이나 체결순서를 변경하더라도 기존 최고가와 손절선을 전면 재구성하지 않는다.

### 수정 원칙

거래 이력이 변경되면 두 종류를 구분해야 한다.

#### 일반 수정

* 메모
* 수수료
* 체결시각의 단순 오타

관련 파생값만 재계산한다.

#### 포지션 구조 수정

* 거래 유형
* 가격
* 수량
* 날짜
* 거래 삭제

전체 거래 이력을 재생하여 다음을 다시 계산한다.

* 보유수량
* 평균단가
* 보유원가
* 실현손익
* 마지막 매수가
* 포지션 시작일
* 최고가 기준 구간

단, 과거 실제 장중 최고가를 복원할 수 없다면 기존 최고가를 임의로 낮추지 않고 사용자에게 재확인을 요구한다.

---

## 4.8 서비스워커가 향후 API 응답을 잘못 처리할 위험

현재 서비스워커는 모든 GET 요청에 대해 네트워크 우선으로 처리하고, 실패하면 캐시 또는 `index.html`로 폴백한다.

서버형 앱에서 같은 전략을 그대로 사용하면 다음 문제가 생길 수 있다.

```text
GET /api/v1/dashboard 실패
→ service worker가 index.html 반환
→ 프런트엔드가 HTML을 JSON으로 해석
→ 예외 발생
```

또한 계좌·자산 API 응답을 서비스워커 캐시에 저장하면 민감한 데이터가 브라우저 캐시에 남을 수 있다.

### 수정 원칙

* `/api/` 요청: Network Only
* 페이지 이동: Network First, 실패 시 `index.html`
* 정적 CSS·JS·아이콘: Cache First 또는 Stale While Revalidate
* 자산·거래·신호 API 응답: 캐시 금지
* 오프라인 화면은 별도로 저장한 최소 스냅샷만 사용

---

## 4.9 app.js 단일 파일 구조

현재 `app.js`에는 다음이 모두 들어 있다.

* 저장소
* 계산
* DOM 렌더링
* 이벤트
* 환율 API
* 데이터 마이그레이션
* 거래 처리
* 신호 판정

기존 앱 규모에서는 작동하지만, 서버 기능이 추가되면 유지보수가 어려워진다.

### 프런트엔드 권장 분리

```text
frontend/
├─ index.html
├─ css/
│  └─ style.css
├─ js/
│  ├─ api-client.js
│  ├─ dashboard.js
│  ├─ instrument-detail.js
│  ├─ trade-form.js
│  ├─ deposits.js
│  ├─ formatters.js
│  ├─ offline-snapshot.js
│  └─ legacy-export.js
├─ manifest.json
└─ service-worker.js
```

프레임워크나 빌드 도구는 도입하지 않아도 된다. ES Modules만 사용하면 충분하다.

---

## 4.10 ATRsite와 Jina의 수동 이중 관리

개발 이력상 ATRsite와 Jina는 코드가 동일하지만 별도 저장소에서 수동 동기화되고 있다.

이 구조는 한쪽에 수정이 누락될 가능성이 높다.

### 수정 원칙

하나의 저장소를 사용하고 환경설정으로 이름만 바꾼다.

```text
APP_NAME=ATRsite
APP_SHORT_NAME=ATR

또는

APP_NAME=Jina
APP_SHORT_NAME=Jina
```

독립 배포가 필요해도 동일 커밋을 서로 다른 환경설정으로 배포한다.

---

# 5. ATRsite-pro 최종 아키텍처

## 5.1 논리 구조

```text
모바일 PWA
   │
   │ HTTPS
   ▼
Reverse Proxy
   │
   ├──────── 정적 HTML/CSS/JS
   │
   ▼
FastAPI Web
   │
   ├─ 종목·거래·예금 API
   ├─ 대시보드 API
   ├─ 수동 데이터 보정
   └─ 상태·이력 조회
   │
   ▼
SQLite WAL
   ▲
   │
Market Worker
   ├─ KIS 토큰 관리
   ├─ 현재가·당일 고가 수집
   ├─ 확정 일봉 수집
   ├─ ATR 계산
   ├─ 신호 상태 판정
   └─ 알림 Outbox 생성
   │
   ▼
Telegram Bot

Backup Timer
   │
   ▼
SQLite Online Backup
   │
   ▼
OCI Object Storage
```

## 5.2 프로세스 결정

### 기본 배포: 두 프로세스

1. `atrsite-web`
2. `atrsite-worker`

둘은 하나의 코드 저장소와 공통 Core 모듈을 사용한다.

### 분리를 유지하는 이유

* 한투 API 장애가 웹 화면까지 정지시키지 않음
* 웹 재배포 중에도 장중 감시 유지 가능
* Worker 오류만 별도로 재시작 가능
* 원인을 빠르게 구분 가능
* 해외장 Worker를 나중에 추가하기 쉬움
* 스케줄러 중복 실행 위험 감소

### 통신 방식

외부 웹훅이나 localhost HTTP 호출은 사용하지 않는다.

두 프로세스가 같은 SQLite DB를 사용한다.

* 웹: 거래·설정·예금 입력
* Worker: 시세·ATR·신호·알림 상태 입력
* 모든 트랜잭션은 짧게 유지

SQLite WAL은 Reader와 Writer가 동시에 진행될 수 있지만 Writer는 한 번에 하나뿐이다. 따라서 WAL 적용만으로 모든 경합이 사라지는 것은 아니며, 짧은 트랜잭션과 `busy_timeout`이 함께 필요하다.

### AMD 1GB 축소 모드

실제 메모리 부족이 확인될 때만 웹과 Worker를 하나의 프로세스로 실행할 수 있다.

단, 코드 모듈은 계속 분리하고 실행 진입점만 통합한다.

---

# 6. 권장 기술 스택

## 서버

* Python 3.12
* FastAPI
* Uvicorn 단일 Worker
* 표준 `sqlite3` 또는 경량 Repository 계층
* `httpx` 또는 기존 KIS API 클라이언트
* systemd
* Caddy 또는 Nginx
* Telegram Bot API

## 프런트엔드

* HTML
* CSS
* 순수 JavaScript ES Modules
* PWA
* 기존 라이트 테마 유지

## 사용하지 않을 구성요소

* React
* Vue
* Node 빌드 시스템
* Redis
* Celery
* PostgreSQL
* Kubernetes
* 유료 모니터링
* 유료 데이터 API

---

# 7. 권장 디렉터리 구조

```text
atrsite-pro/
├─ backend/
│  └─ atrsite/
│     ├─ main.py
│     ├─ worker.py
│     ├─ config.py
│     ├─ db.py
│     ├─ schema.py
│     ├─ repositories/
│     │  ├─ instruments.py
│     │  ├─ trades.py
│     │  ├─ market_data.py
│     │  ├─ signals.py
│     │  └─ notifications.py
│     ├─ services/
│     │  ├─ position_engine.py
│     │  ├─ atr_engine.py
│     │  ├─ signal_engine.py
│     │  ├─ market_schedule.py
│     │  ├─ portfolio_service.py
│     │  └─ backup_service.py
│     ├─ adapters/
│     │  ├─ kis_client.py
│     │  ├─ telegram_client.py
│     │  └─ fx_client.py
│     └─ api/
│        ├─ dashboard.py
│        ├─ instruments.py
│        ├─ trades.py
│        ├─ deposits.py
│        └─ health.py
├─ frontend/
│  ├─ index.html
│  ├─ css/
│  ├─ js/
│  ├─ manifest.json
│  └─ service-worker.js
├─ migrations/
├─ scripts/
│  ├─ backup.py
│  ├─ restore_check.py
│  └─ import_legacy.py
├─ systemd/
│  ├─ atrsite-web.service
│  ├─ atrsite-worker.service
│  ├─ atrsite-backup.service
│  └─ atrsite-backup.timer
├─ tests/
├─ data/
└─ backups/
```

---

# 8. 계산 엔진 최종 명세

## 8.1 평균단가와 보유원가

이동평균 원가법을 사용한다.

### 매수

```text
새 보유수량 = 기존 보유수량 + 매수수량
새 보유원가 = 기존 보유원가 + 매수가 × 매수수량 + 매수수수료
새 평균단가 = 새 보유원가 ÷ 새 보유수량
```

### 매도

```text
매도 직전 평균단가 = 기존 보유원가 ÷ 기존 보유수량
차감 원가 = 매도 직전 평균단가 × 매도수량
새 보유원가 = 기존 보유원가 - 차감 원가
새 보유수량 = 기존 보유수량 - 매도수량
실현손익 = 매도가 × 매도수량 - 차감 원가 - 수수료 - 세금
```

전량 매도 시 부동소수점 잔여 오차를 막기 위해 다음 값을 0으로 정규화한다.

* 보유수량
* 보유원가
* 평균단가

## 8.2 다음 매수가

```text
다음 매수가 =
마지막 매수가 - 확정 ATR × 매수배수
```

조건:

* 마지막 매수가가 있어야 함
* 최초 진입 전에는 별도의 기준가격이 필요함
* 최초 기준가격은 사용자 지정 또는 현재가 기준으로 설정
* 부분매도는 다음 매수가 기준을 변경하지 않음

## 8.3 익절가

```text
익절가 =
현재 평균단가 + 확정 ATR × 매도배수
```

조건:

* 보유수량이 0보다 클 때만 활성화

## 8.4 추적 손절선

```text
후보 손절선 =
보유 후 최고가 - 확정 ATR × 손절배수

새 손절선 =
MAX(기존 손절선, 후보 손절선)
```

조건:

* 보유수량이 0보다 클 때만 활성화
* 부분매도 후에도 유지
* 전량 매도 후 초기화
* 새 포지션 진입 시 새 사이클 시작
* 서버 재시작 후에도 DB에 저장된 값을 복원

## 8.5 ATR 정책

### 기본 정책

* Wilder ATR(14)
* 수정주가 기반 일봉
* 확정된 일봉만 사용
* 장중에는 전일까지 확정된 ATR 사용
* 장 마감 후 새 일봉을 반영
* 새 ATR은 다음 거래일부터 적용

장중 미완성 봉으로 신호용 ATR을 계속 바꾸지 않는다.

## 8.6 `atrAtExecution`

기존 코드가 저장하고 있는 체결 당시 ATR은 유지한다.

용도:

* 거래 당시 판단 근거 기록
* 사후 전략 검토
* 신호 재현
* 설정 변경 전후 비교

현재 기준선 계산은 최신 확정 ATR을 사용하고, `atrAtExecution`은 감사 이력으로만 사용한다.

---

# 9. 신호 상태 설계

## 9.1 거래 신호와 데이터 상태 분리

다음 두 상태를 혼합해서는 안 된다.

### 거래 신호

* `NO_POSITION`
* `NEUTRAL`
* `BUY_TRIGGERED`
* `TAKE_PROFIT_TRIGGERED`
* `STOP_TRIGGERED`
* `SELL_BOTH_TRIGGERED`

### 데이터 상태

* `FRESH`
* `DELAYED`
* `STALE`
* `MARKET_CLOSED`
* `API_ERROR`
* `INSUFFICIENT_DATA`
* `MANUAL_OVERRIDE`

데이터 상태가 `STALE`, `API_ERROR`, `INSUFFICIENT_DATA`이면 신규 거래 신호를 발송하지 않는다.

## 9.2 신호 우선순위

```text
1. 손절선 + 익절가 동시 도달
2. 손절선 도달
3. 익절가 도달
4. 매수가 도달
5. 관망
```

## 9.3 중복 알림 방지

다음 조건일 때만 신규 이벤트를 만든다.

```text
이전 신호 상태 != 새 신호 상태
```

각 이벤트에는 고유 `event_id`를 부여한다.

## 9.4 기준선 반복 통과 방지

기준선 주변에서 가격이 흔들릴 때 반복 알림을 막기 위해 히스테리시스를 둔다.

초기 예시:

```text
매수 신호 진입:
현재가 ≤ 매수 기준가

매수 신호 해제:
현재가 ≥ 매수 기준가 + 0.1 ATR
```

0.1 ATR은 초기값이며 실제 사용 기록을 보고 조정한다.

---

# 10. 데이터베이스 설계

## 10.1 핵심 테이블

| 테이블                   | 역할                 |
| --------------------- | ------------------ |
| `schema_meta`         | DB 스키마 버전          |
| `instruments`         | 종목, 시장, 통화, API 코드 |
| `strategy_settings`   | ATR 배수와 설정 버전      |
| `trades`              | 실제 수동 체결           |
| `position_state`      | 현재 수량·원가·평균단가      |
| `daily_bars`          | ATR 계산용 확정 일봉      |
| `atr_values`          | 날짜별 ATR            |
| `quote_latest`        | 종목별 최신 시세          |
| `signal_state`        | 현재 신호 상태           |
| `signal_events`       | 신호 전환 이력           |
| `notification_outbox` | 텔레그램 발송 대기         |
| `deposits`            | 현금·예금              |
| `fx_rates`            | 환율                 |
| `sync_runs`           | 시세 수집 성공·실패        |
| `audit_log`           | 중요 설정·데이터 변경 이력    |

## 10.2 주요 제약조건

```text
daily_bars:
UNIQUE(instrument_id, trade_date)

atr_values:
UNIQUE(instrument_id, trade_date, period)

notification_outbox:
UNIQUE(signal_event_id, channel)

trades:
CHECK(price > 0)
CHECK(quantity > 0)
```

매도수량 초과 여부는 DB 제약만으로 처리하기 어려우므로 서비스 계층에서 전체 거래 순서를 검증한다.

## 10.3 SQLite 설정

```text
journal_mode = WAL
foreign_keys = ON
busy_timeout = 5000
synchronous = NORMAL
```

WAL 파일이 계속 커지지 않도록 장 마감 후 체크포인트를 수행한다. SQLite 공식 문서도 장시간 Reader가 유지되면 WAL 체크포인트가 완료되지 못해 파일이 계속 커질 수 있다고 설명한다.

---

# 11. 한국투자증권 API 운영

## 11.1 토큰

개인·일반법인용 접근토큰은 유효기간 24시간이며, 발급 응답의 만료시각을 사용해 관리해야 한다. 1일 1회 발급 원칙과 6시간 갱신 발급 주기도 공식 문서에 안내되어 있다.

### 토큰 처리 순서

```text
저장된 토큰 확인
→ 만료시각 확인
→ 유효하면 재사용
→ 만료 임박 시 재발급
→ 인증 오류 발생 시 1회 재발급 후 재시도
```

서버가 재시작될 때마다 새 토큰을 발급하지 않는다.

## 11.2 호출 속도

2026년 4월 20일 기준 공식 공지에는 실전 REST API가 계좌당 초당 18건, 동시 호출 시 100~150ms 간격 권장이 안내되어 있다. ATRsite-pro의 5분 폴링은 이 한도보다 훨씬 낮지만, 모든 호출은 순차 Rate Limiter를 거치게 한다.

## 11.3 초기 폴링 정책

초기 버전은 복잡한 스마트 폴링보다 안정성을 우선한다.

```text
정규장:
5분마다 현재가와 당일 고가 조회

장 시작 전:
토큰·거래일·종목 상태 확인

장 마감 후:
확정 일봉 수집
ATR 갱신
DB 체크포인트
백업 수행
```

1분 폴링은 운영 안정성이 확인된 뒤 다음 조건에서만 선택적으로 추가한다.

```text
현재가가 매수·익절·손절 기준 중 하나에서
0.25 ATR 이내
```

## 11.4 거래일 판정

단순히 월요일부터 금요일까지만 검사하면 안 된다.

* 공휴일
* 임시 휴장
* 수능일 개장시간 변경
* 조기 종료
* 해외시장 서머타임

시장별 거래일 일정 테이블과 수동 예외 설정을 둔다.

---

# 12. 알림 시스템

## 12.1 Outbox 구조

신호 저장과 텔레그램 발송을 직접 연결하지 않는다.

한 DB 트랜잭션에서 다음을 함께 기록한다.

1. `signal_events`
2. `notification_outbox`

알림 Worker는 Outbox를 읽어 발송한다.

```text
PENDING
→ SENDING
→ SENT

또는

PENDING
→ FAILED
→ RETRY
```

## 12.2 재시도

예시:

```text
1차 실패: 30초 후
2차 실패: 2분 후
3차 실패: 10분 후
4차 실패: 실패 확정
```

장중 장애 알림 자체가 반복되지 않도록 동일 오류 유형에도 쿨다운을 둔다.

## 12.3 텔레그램 메시지

```text
[ATR 매수 기준 도달]

삼성전자
현재가: 70,000
매수 기준가: 70,200
기준 대비: -0.28%
적용 ATR: 1,240
ATR 기준일: 2026-07-31

등록된 트랜치 금액: 700,000
계산 가능 수량: 10주

실제 주문은 MTS에서 직접 확인
```

추천 수량이라는 표현은 사용하지 않는다.

* 등록된 트랜치 기준 수량
* 설정 기준 계산 수량
* 예정 수량

중 하나로 표현한다.

---

# 13. 모바일 UI 개편안

## 13.1 유지할 부분

현재 UI에서 다음은 유지한다.

* 모바일 카드형 목록
* 5단계 신호 색상
* 숫자용 모노스페이스 글꼴
* 종목 상세 화면
* 하단 매수·매도 체결 등록 버튼
* 하락 구간표
* 부분매도 시뮬레이션
* 예금 관리
* 통화 전환
* 거래 수정·삭제
* 라이트 테마

현재 HTML과 CSS는 이미 모바일 우선 구조와 충분한 버튼 높이를 갖추고 있다.

## 13.2 오늘의 입력 영역 변경

기존:

```text
현재가 직접 입력
ATR 직접 입력
최고가 직접 입력
```

변경:

```text
현재가: 70,000
조회시각: 10:25:02
데이터 상태: 정상
당일 고가: 71,200
적용 ATR: 1,240
ATR 기준일: 2026-07-31
데이터 출처: 한국투자증권
```

아래에 별도로 둔다.

```text
수동 보정
```

수동값을 사용하면 자동값과 명확히 구분한다.

## 13.3 기준선까지 거리

```text
매수 기준까지: 700원 / 1.00% / 0.56 ATR
익절 기준까지: 2,100원 / 3.00% / 1.69 ATR
손절선까지: 1,400원 / 2.00% / 1.13 ATR
```

## 13.4 체결 완료 버튼

기존 `매수 추가`, `매도 추가`는 다음 의미로 명확히 변경한다.

```text
매수 체결 등록
매도 체결 등록
```

입력항목:

* 실제 체결가
* 실제 체결수량
* 체결일시
* 수수료
* 세금
* 메모

## 13.5 오프라인 모드

오프라인에서는 다음만 허용한다.

* 마지막 대시보드 열람
* 마지막 거래 이력 열람
* 마지막 기준가 확인

다음은 금지한다.

* 신규 체결 저장
* 설정 변경
* 최신 신호로 오인할 수 있는 계산
* 서버 동기화가 필요한 작업

화면 상단에 다음을 고정 표시한다.

```text
오프라인 저장본
마지막 동기화: 2026-08-01 10:25
현재 매매 판단용으로 사용하지 마십시오.
```

---

# 14. 보안 설계

## 14.1 최소 접근통제

복잡한 회원가입은 만들지 않는다.

다음만 적용한다.

* HTTPS
* 단일 사용자 Basic Auth 또는 단일 세션 인증
* API의 동일 출처 사용
* 쓰기 요청 CSRF 방지
* CORS 외부 차단
* 일정 횟수 인증 실패 시 일시 차단

## 14.2 비밀정보

다음은 프런트엔드·DB·Git 저장소에 넣지 않는다.

* KIS APP Key
* KIS APP Secret
* Access Token
* Telegram Bot Token

서버의 권한 제한 설정파일 또는 환경변수에 저장한다.

```text
파일 권한: 600
소유자: atrsite 서비스 계정
```

## 14.3 로그

로그에 다음 값을 기록하지 않는다.

* APP Secret
* Access Token
* Telegram Token
* 계좌번호 전체
* 인증 헤더
* API 원문 전체 응답

---

# 15. 백업 및 복구

## 15.1 보관 정책

### 서버 내부

* 최신 2개만 보관

### OCI Object Storage

* 최근 14개만 보관
* 전체 백업 최대 500MB
* 최소 3개는 항상 유지
* 14일 또는 용량 상한을 넘으면 오래된 순서로 삭제

Oracle Always Free Object Storage는 공식 문서상 합계 20GB와 월 50,000회 API 요청을 제공한다. 하루 한 번 업로드와 순환 삭제는 이 한도에 비해 매우 적은 사용량이다.

## 15.2 백업 순서

```text
SQLite Online Backup 생성
→ PRAGMA integrity_check
→ 외래키 검사
→ gzip 압축
→ Object Storage 업로드
→ 업로드 크기·해시 확인
→ 로컬 최신 2개 외 삭제
→ 원격 14개 초과분 삭제
```

신규 백업이 실패하면 기존 백업을 삭제하지 않는다.

SQLite 파일을 단순 복사하지 않고 Online Backup API를 사용한다. 공식 SQLite 문서도 실행 중인 DB의 일관된 복사본을 만들기 위한 Online Backup API를 제공한다.

`PRAGMA integrity_check`는 테이블·인덱스·페이지 구조의 일관성 오류를 검사하며, 정상일 때 `ok`를 반환한다.

## 15.3 복원 점검

매월 한 번 다음을 자동 또는 수동으로 확인한다.

* 최신 백업 다운로드
* 임시 DB로 복원
* 무결성 검사
* 핵심 테이블 행 수 확인
* 애플리케이션 읽기 테스트
* 임시 복원본 삭제

복원 테스트를 위해 월간 백업을 추가 보관할 필요는 없다.

---

# 16. 기존 localStorage 데이터 이전

## 16.1 직접 접근 불가 문제

Vercel 도메인과 Oracle 서버 도메인이 다르면 새 서버는 기존 브라우저 localStorage를 읽을 수 없다.

따라서 기존 ATRsite에 데이터 내보내기 기능을 추가한다.

## 16.2 이전 절차

```text
기존 앱에서 JSON 내보내기
→ 사용자 파일 저장
→ ATRsite-pro에서 가져오기
→ 사전 검증 결과 표시
→ 사용자 확인
→ DB 트랜잭션으로 저장
→ 가져오기 결과 보고
```

## 16.3 내보내기 대상

* `stock-portfolio`
* `cash-deposits`
* `fx-rates`
* 앱 버전
* 내보낸 시각
* 데이터 스키마 버전
* 체크섬

## 16.4 가져오기 검증

* 중복 ID
* 잘못된 거래유형
* 음수 수량
* 보유수량 초과 매도
* 날짜 형식
* 통화 형식
* 누락된 ATR
* 비정상 평균단가
* 기존 추적 손절선

현재 코드에 이미 구형 코스피200·QQQ 데이터를 새 종목 구조로 변환하는 마이그레이션 경험이 있으므로, 이 원칙을 서버 이전에도 확장할 수 있다.

---

# 17. 장애 복구

## 17.1 서버 재시작 순서

```text
DB 연결 확인
→ 스키마 버전 확인
→ 무결성 간이 검사
→ 종목과 설정 로드
→ 거래 이력 검증
→ 포지션 상태 재계산
→ 레칫 손절선 복원
→ 마지막 일봉 날짜 확인
→ 누락 일봉 보충
→ ATR 확인
→ 미발송 Outbox 재처리
→ 시장 운영 여부 확인
→ 시세 감시 시작
```

## 17.2 systemd

별도 서비스로 운영한다.

```text
atrsite-web.service
atrsite-worker.service
atrsite-backup.timer
```

기본 정책:

```text
Restart=on-failure
RestartSec=10
```

반복 실패 시 무한 재시작을 막기 위해 시작 횟수 제한을 둔다.

## 17.3 데이터 지연

5분 폴링 기준 예시:

|     경과시간 | 상태      |
| -------: | ------- |
|     0~7분 | 정상      |
|    7~15분 | 지연      |
|   15분 초과 | 오래된 데이터 |
| 연속 3회 실패 | API 장애  |

오래된 데이터에서는 마지막 신호를 삭제하지 않는다.

대신 다음처럼 표시한다.

```text
이 신호는 마지막 정상 데이터 기준이며 현재 유효성을 확인할 수 없습니다.
```

---

# 18. 구현 단계

## 1단계: 계산 엔진 교정

먼저 서버를 만들기 전에 다음을 수정하고 테스트한다.

* 평균단가
* 보유원가
* 실현손익
* 초과매도 차단
* 마지막 매수가
* 전량매도 초기화
* 레칫 손절선

## 2단계: 서버 수동형 복제

기존 화면과 기능을 서버 DB로 이전하되 시세는 아직 수동 입력한다.

목표:

* 기존 앱과 계산 결과 일치
* localStorage 제거
* SQLite 저장
* 백업·복원 확인

## 3단계: 자동 시세 수집

* KIS 토큰
* 현재가
* 당일 고가
* 확정 일봉
* ATR 자동 계산

이 단계에서는 텔레그램 신호를 발송하지 않고 화면에만 표시한다.

## 4단계: Shadow Mode

1~2주 동안 다음을 비교한다.

* 기존 수동 현재가
* 자동 현재가
* 기존 수동 ATR
* 서버 ATR
* 기존 신호
* 서버 신호

차이가 나면 원인을 기록한다.

## 5단계: 텔레그램 알림

* 상태 변경 알림
* 중복 차단
* Outbox 재전송
* 장애·복구 알림

## 6단계: 운영 전환

* 기존 Vercel 버전은 일정 기간 읽기·비상용으로 유지
* 신규 거래는 ATRsite-pro에만 입력
* 안정화 후 기존 버전은 데이터 내보내기 전용으로 전환

---

# 19. 필수 테스트

## 계산 테스트

1. 단일 매수
2. 복수 매수
3. 부분매도
4. 전량매도
5. 부분매도 후 재매수
6. 여러 번의 매수·매도 반복
7. 보유수량 초과 매도
8. 거래 수정 후 재계산
9. 거래 삭제 후 재계산
10. 같은 날 여러 거래 순서

## 신호 테스트

1. 미보유 상태에서 익절 신호가 발생하지 않음
2. 미보유 상태에서 손절 신호가 발생하지 않음
3. 매수 기준 도달
4. 익절 기준 도달
5. 손절 기준 도달
6. 손절·익절 동시 충족
7. 반복 폴링 시 중복 알림 없음
8. 관망 후 재진입 시 새 이벤트 발생
9. 데이터 지연 시 신규 신호 차단
10. 서버 재시작 후 동일 상태 복원

## 레칫 테스트

```text
최고가 100, ATR 10, 배수 2
손절선 80

최고가 110
손절선 90

ATR 20으로 증가
후보 70
저장 손절선은 90 유지
```

## 백업 테스트

* 백업 생성
* 무결성 검사
* 업로드 성공
* 14개 초과분 삭제
* 업로드 실패 시 기존 백업 유지
* 복원 후 데이터 일치

---

# 20. 기능 우선순위

## 반드시 구현

1. 평균단가 계산 수정
2. 초과매도 차단
3. 포지션 상태 기반 신호
4. 마지막 매수가 기준 적용
5. 서버 DB 이전
6. ATR 확정 일봉 정책
7. 시세 신선도 표시
8. 추적 손절선 영속화
9. 신호 이벤트와 Outbox
10. 토큰 만료 관리
11. systemd 재시작
12. SQLite WAL
13. 순환 백업
14. localStorage 내보내기·가져오기
15. API와 서비스워커 캐시 분리
16. HTTPS와 단일 사용자 인증
17. 계산 단위 테스트

## 운영 후 추가

1. 기준선 근접 종목 1분 폴링
2. 텔레그램 상세화면 링크
3. 손절선 상승 뱃지
4. 설정 변경 이력 비교
5. API 잔고와 앱 수량 불일치 표시
6. ATR 계산 과정 상세보기
7. 일별 신호 요약
8. 거래 메모와 태그
9. 해외시장 거래일 일정
10. 장중 예상 ATR 참고 표시

## 제외

1. 자동 주문
2. AI 매매 추천
3. 앱이 임의로 정하는 투자비중
4. 회원가입
5. 여러 사용자 계좌
6. 소셜 로그인
7. 실시간 초단위 틱 저장
8. 모든 5분 봉 무기한 보관
9. 텔레그램 DB 백업
10. 복잡한 분산 시스템

---

# 21. 최종 판단

기존 ATRsite의 UI와 사용자 흐름은 전면 폐기할 이유가 없다. 오히려 현재 코드에서 다음 자산은 그대로 계승하는 것이 낫다.

* 모바일 중심 화면 구성
* 종목 등록형 구조
* 5단계 신호 표시
* 하락 구간표
* 부분매도 시뮬레이션
* 거래 이력 수정
* 예금과 환율 관리
* PWA 설치 경험

반면 다음 영역은 기존 JavaScript 구현을 그대로 서버로 옮겨서는 안 된다.

* 평균단가와 실현손익
* 포지션 무결성 검증
* 신호 상태 관리
* 최고가 관리
* 데이터 신선도
* 알림 중복 방지
* 장애 복구
* 백업
* 인증
* 서비스워커의 API 처리

따라서 ATRsite-pro는 기존 앱에 시세 API를 단순히 붙이는 프로젝트가 아니라 다음과 같이 정의해야 한다.

> 기존 ATRsite의 검증된 모바일 UX를 유지하면서, 계산·상태·이력·장애 복구를 서버에서 일관되게 관리하는 개인용 ATR 매매 의사결정 시스템

이 구조라면 자동매매 금지, 1인용, 모바일 우선, 무료 운영이라는 네 가지 원칙을 유지하면서도 실제 장중 사용에 필요한 신뢰성과 복구 가능성을 확보할 수 있다.
